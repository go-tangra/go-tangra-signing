import {
  __commonJS
} from "./chunk-72XF4MIR.js";

// node_modules/.pnpm/@module-federation+runtime@2.2.2/node_modules/@module-federation/runtime/dist/_virtual/_rolldown/runtime.cjs
var require_runtime = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime@2.2.2/node_modules/@module-federation/runtime/dist/_virtual/_rolldown/runtime.cjs"(exports2) {
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
          key = keys[i];
          if (!__hasOwnProp.call(to, key) && key !== except) {
            __defProp(to, key, {
              get: ((k) => from[k]).bind(null, key),
              enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
            });
          }
        }
      }
      return to;
    };
    var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
      value: mod,
      enumerable: true
    }) : target, mod));
    exports2.__toESM = __toESM;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/constant.cjs
var require_constant = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/constant.cjs"(exports2) {
    var FederationModuleManifest = "federation-manifest.json";
    var MANIFEST_EXT = ".json";
    var BROWSER_LOG_KEY = "FEDERATION_DEBUG";
    var NameTransformSymbol = {
      AT: "@",
      HYPHEN: "-",
      SLASH: "/"
    };
    var NameTransformMap = {
      [NameTransformSymbol.AT]: "scope_",
      [NameTransformSymbol.HYPHEN]: "_",
      [NameTransformSymbol.SLASH]: "__"
    };
    var EncodedNameTransformMap = {
      [NameTransformMap[NameTransformSymbol.AT]]: NameTransformSymbol.AT,
      [NameTransformMap[NameTransformSymbol.HYPHEN]]: NameTransformSymbol.HYPHEN,
      [NameTransformMap[NameTransformSymbol.SLASH]]: NameTransformSymbol.SLASH
    };
    var SEPARATOR = ":";
    var ManifestFileName = "mf-manifest.json";
    var StatsFileName = "mf-stats.json";
    var MFModuleType = {
      NPM: "npm",
      APP: "app"
    };
    var MODULE_DEVTOOL_IDENTIFIER = "__MF_DEVTOOLS_MODULE_INFO__";
    var ENCODE_NAME_PREFIX = "ENCODE_NAME_PREFIX";
    var TEMP_DIR = ".federation";
    var MFPrefetchCommon = {
      identifier: "MFDataPrefetch",
      globalKey: "__PREFETCH__",
      library: "mf-data-prefetch",
      exportsKey: "__PREFETCH_EXPORTS__",
      fileName: "bootstrap.js"
    };
    var TreeShakingStatus = (function(TreeShakingStatus2) {
      TreeShakingStatus2[TreeShakingStatus2["UNKNOWN"] = 1] = "UNKNOWN";
      TreeShakingStatus2[TreeShakingStatus2["CALCULATED"] = 2] = "CALCULATED";
      TreeShakingStatus2[TreeShakingStatus2["NO_USE"] = 0] = "NO_USE";
      return TreeShakingStatus2;
    })({});
    exports2.BROWSER_LOG_KEY = BROWSER_LOG_KEY;
    exports2.ENCODE_NAME_PREFIX = ENCODE_NAME_PREFIX;
    exports2.EncodedNameTransformMap = EncodedNameTransformMap;
    exports2.FederationModuleManifest = FederationModuleManifest;
    exports2.MANIFEST_EXT = MANIFEST_EXT;
    exports2.MFModuleType = MFModuleType;
    exports2.MFPrefetchCommon = MFPrefetchCommon;
    exports2.MODULE_DEVTOOL_IDENTIFIER = MODULE_DEVTOOL_IDENTIFIER;
    exports2.ManifestFileName = ManifestFileName;
    exports2.NameTransformMap = NameTransformMap;
    exports2.NameTransformSymbol = NameTransformSymbol;
    exports2.SEPARATOR = SEPARATOR;
    exports2.StatsFileName = StatsFileName;
    exports2.TEMP_DIR = TEMP_DIR;
    exports2.TreeShakingStatus = TreeShakingStatus;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/_virtual/_rolldown/runtime.cjs
var require_runtime2 = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/_virtual/_rolldown/runtime.cjs"(exports2) {
    var __defProp = Object.defineProperty;
    var __exportAll = (all, no_symbols) => {
      let target = {};
      for (var name in all) {
        __defProp(target, name, {
          get: all[name],
          enumerable: true
        });
      }
      if (!no_symbols) {
        __defProp(target, Symbol.toStringTag, { value: "Module" });
      }
      return target;
    };
    exports2.__exportAll = __exportAll;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ContainerPlugin.cjs
var require_ContainerPlugin = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ContainerPlugin.cjs"(exports2) {
    var require_runtime4 = require_runtime2();
    var ContainerPlugin_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "ContainerPlugin_exports", {
      enumerable: true,
      get: function() {
        return ContainerPlugin_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ContainerReferencePlugin.cjs
var require_ContainerReferencePlugin = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ContainerReferencePlugin.cjs"(exports2) {
    var require_runtime4 = require_runtime2();
    var ContainerReferencePlugin_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "ContainerReferencePlugin_exports", {
      enumerable: true,
      get: function() {
        return ContainerReferencePlugin_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ModuleFederationPlugin.cjs
var require_ModuleFederationPlugin = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ModuleFederationPlugin.cjs"(exports2) {
    var require_runtime4 = require_runtime2();
    var ModuleFederationPlugin_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "ModuleFederationPlugin_exports", {
      enumerable: true,
      get: function() {
        return ModuleFederationPlugin_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/SharePlugin.cjs
var require_SharePlugin = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/SharePlugin.cjs"(exports2) {
    var require_runtime4 = require_runtime2();
    var SharePlugin_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "SharePlugin_exports", {
      enumerable: true,
      get: function() {
        return SharePlugin_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ConsumeSharedPlugin.cjs
var require_ConsumeSharedPlugin = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ConsumeSharedPlugin.cjs"(exports2) {
    var require_runtime4 = require_runtime2();
    var ConsumeSharedPlugin_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "ConsumeSharedPlugin_exports", {
      enumerable: true,
      get: function() {
        return ConsumeSharedPlugin_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ProvideSharedPlugin.cjs
var require_ProvideSharedPlugin = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/types/plugins/ProvideSharedPlugin.cjs"(exports2) {
    var require_runtime4 = require_runtime2();
    var ProvideSharedPlugin_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "ProvideSharedPlugin_exports", {
      enumerable: true,
      get: function() {
        return ProvideSharedPlugin_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/env.cjs
var require_env = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/env.cjs"(exports2) {
    var require_constant3 = require_constant();
    var isBrowserEnvValue = typeof ENV_TARGET !== "undefined" ? ENV_TARGET === "web" : typeof window !== "undefined" && typeof window.document !== "undefined";
    function isBrowserEnv() {
      return isBrowserEnvValue;
    }
    function isReactNativeEnv() {
      return typeof navigator !== "undefined" && (navigator == null ? void 0 : navigator.product) === "ReactNative";
    }
    function isBrowserDebug() {
      try {
        if (isBrowserEnv() && window.localStorage) return Boolean(localStorage.getItem(require_constant3.BROWSER_LOG_KEY));
      } catch (error) {
        return false;
      }
      return false;
    }
    function isDebugMode() {
      if (typeof process !== "undefined" && process.env && process.env["FEDERATION_DEBUG"]) return Boolean(process.env["FEDERATION_DEBUG"]);
      if (typeof FEDERATION_DEBUG !== "undefined" && Boolean(FEDERATION_DEBUG)) return true;
      return isBrowserDebug();
    }
    var getProcessEnv = function() {
      return typeof process !== "undefined" && process.env ? process.env : {};
    };
    exports2.getProcessEnv = getProcessEnv;
    exports2.isBrowserEnv = isBrowserEnv;
    exports2.isBrowserEnvValue = isBrowserEnvValue;
    exports2.isDebugMode = isDebugMode;
    exports2.isReactNativeEnv = isReactNativeEnv;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/utils.cjs
var require_utils = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/utils.cjs"(exports2) {
    var require_constant3 = require_constant();
    var require_env3 = require_env();
    var LOG_CATEGORY = "[ Federation Runtime ]";
    var parseEntry = (str, devVerOrUrl, separator = require_constant3.SEPARATOR) => {
      const strSplit = str.split(separator);
      const devVersionOrUrl = require_env3.getProcessEnv()["NODE_ENV"] === "development" && devVerOrUrl;
      const defaultVersion = "*";
      const isEntry = (s) => s.startsWith("http") || s.includes(require_constant3.MANIFEST_EXT);
      if (strSplit.length >= 2) {
        let [name, ...versionOrEntryArr] = strSplit;
        if (str.startsWith(separator)) {
          name = strSplit.slice(0, 2).join(separator);
          versionOrEntryArr = [devVersionOrUrl || strSplit.slice(2).join(separator)];
        }
        let versionOrEntry = devVersionOrUrl || versionOrEntryArr.join(separator);
        if (isEntry(versionOrEntry)) return {
          name,
          entry: versionOrEntry
        };
        else return {
          name,
          version: versionOrEntry || defaultVersion
        };
      } else if (strSplit.length === 1) {
        const [name] = strSplit;
        if (devVersionOrUrl && isEntry(devVersionOrUrl)) return {
          name,
          entry: devVersionOrUrl
        };
        return {
          name,
          version: devVersionOrUrl || defaultVersion
        };
      } else throw `Invalid entry value: ${str}`;
    };
    var composeKeyWithSeparator = function(...args) {
      if (!args.length) return "";
      return args.reduce((sum, cur) => {
        if (!cur) return sum;
        if (!sum) return cur;
        return `${sum}${require_constant3.SEPARATOR}${cur}`;
      }, "");
    };
    var encodeName = function(name, prefix = "", withExt = false) {
      try {
        const ext = withExt ? ".js" : "";
        return `${prefix}${name.replace(new RegExp(`${require_constant3.NameTransformSymbol.AT}`, "g"), require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.AT]).replace(new RegExp(`${require_constant3.NameTransformSymbol.HYPHEN}`, "g"), require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.HYPHEN]).replace(new RegExp(`${require_constant3.NameTransformSymbol.SLASH}`, "g"), require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.SLASH])}${ext}`;
      } catch (err) {
        throw err;
      }
    };
    var decodeName = function(name, prefix, withExt) {
      try {
        let decodedName = name;
        if (prefix) {
          if (!decodedName.startsWith(prefix)) return decodedName;
          decodedName = decodedName.replace(new RegExp(prefix, "g"), "");
        }
        decodedName = decodedName.replace(new RegExp(`${require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.AT]}`, "g"), require_constant3.EncodedNameTransformMap[require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.AT]]).replace(new RegExp(`${require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.SLASH]}`, "g"), require_constant3.EncodedNameTransformMap[require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.SLASH]]).replace(new RegExp(`${require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.HYPHEN]}`, "g"), require_constant3.EncodedNameTransformMap[require_constant3.NameTransformMap[require_constant3.NameTransformSymbol.HYPHEN]]);
        if (withExt) decodedName = decodedName.replace(".js", "");
        return decodedName;
      } catch (err) {
        throw err;
      }
    };
    var generateExposeFilename = (exposeName, withExt) => {
      if (!exposeName) return "";
      let expose = exposeName;
      if (expose === ".") expose = "default_export";
      if (expose.startsWith("./")) expose = expose.replace("./", "");
      return encodeName(expose, "__federation_expose_", withExt);
    };
    var generateShareFilename = (pkgName, withExt) => {
      if (!pkgName) return "";
      return encodeName(pkgName, "__federation_shared_", withExt);
    };
    var getResourceUrl = (module3, sourceUrl) => {
      if ("getPublicPath" in module3) {
        let publicPath;
        if (!module3.getPublicPath.startsWith("function")) publicPath = new Function(module3.getPublicPath)();
        else publicPath = new Function("return " + module3.getPublicPath)()();
        return `${publicPath}${sourceUrl}`;
      } else if ("publicPath" in module3) {
        if (!require_env3.isBrowserEnv() && !require_env3.isReactNativeEnv() && "ssrPublicPath" in module3 && typeof module3.ssrPublicPath === "string") return `${module3.ssrPublicPath}${sourceUrl}`;
        return `${module3.publicPath}${sourceUrl}`;
      } else {
        console.warn("Cannot get resource URL. If in debug mode, please ignore.", module3, sourceUrl);
        return "";
      }
    };
    var assert = (condition, msg) => {
      if (!condition) error(msg);
    };
    var error = (msg) => {
      throw new Error(`${LOG_CATEGORY}: ${msg}`);
    };
    var warn = (msg) => {
      console.warn(`${LOG_CATEGORY}: ${msg}`);
    };
    function safeToString(info) {
      try {
        return JSON.stringify(info, null, 2);
      } catch (e) {
        return "";
      }
    }
    var VERSION_PATTERN_REGEXP = /^([\d^=v<>~]|[*xX]$)/;
    function isRequiredVersion(str) {
      return VERSION_PATTERN_REGEXP.test(str);
    }
    exports2.assert = assert;
    exports2.composeKeyWithSeparator = composeKeyWithSeparator;
    exports2.decodeName = decodeName;
    exports2.encodeName = encodeName;
    exports2.error = error;
    exports2.generateExposeFilename = generateExposeFilename;
    exports2.generateShareFilename = generateShareFilename;
    exports2.getResourceUrl = getResourceUrl;
    exports2.isRequiredVersion = isRequiredVersion;
    exports2.parseEntry = parseEntry;
    exports2.safeToString = safeToString;
    exports2.warn = warn;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/generateSnapshotFromManifest.cjs
var require_generateSnapshotFromManifest = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/generateSnapshotFromManifest.cjs"(exports2) {
    var require_constant3 = require_constant();
    var simpleJoinRemoteEntry = (rPath, rName) => {
      if (!rPath) return rName;
      const transformPath = (str) => {
        if (str === ".") return "";
        if (str.startsWith("./")) return str.replace("./", "");
        if (str.startsWith("/")) {
          const strWithoutSlash = str.slice(1);
          if (strWithoutSlash.endsWith("/")) return strWithoutSlash.slice(0, -1);
          return strWithoutSlash;
        }
        return str;
      };
      const transformedPath = transformPath(rPath);
      if (!transformedPath) return rName;
      if (transformedPath.endsWith("/")) return `${transformedPath}${rName}`;
      return `${transformedPath}/${rName}`;
    };
    function inferAutoPublicPath(url2) {
      return url2.replace(/#.*$/, "").replace(/\?.*$/, "").replace(/\/[^\/]+$/, "/");
    }
    function generateSnapshotFromManifest(manifest, options = {}) {
      var _a, _b, _c;
      const { remotes = {}, overrides = {}, version } = options;
      let remoteSnapshot;
      const getPublicPath = () => {
        if ("publicPath" in manifest.metaData) {
          if ((manifest.metaData.publicPath === "auto" || manifest.metaData.publicPath === "") && version) return inferAutoPublicPath(version);
          return manifest.metaData.publicPath;
        } else return manifest.metaData.getPublicPath;
      };
      const overridesKeys = Object.keys(overrides);
      let remotesInfo = {};
      if (!Object.keys(remotes).length) remotesInfo = ((_a = manifest.remotes) == null ? void 0 : _a.reduce((res2, next) => {
        let matchedVersion;
        const name = next.federationContainerName;
        if (overridesKeys.includes(name)) matchedVersion = overrides[name];
        else if ("version" in next) matchedVersion = next.version;
        else matchedVersion = next.entry;
        res2[name] = { matchedVersion };
        return res2;
      }, {})) || {};
      Object.keys(remotes).forEach((key) => remotesInfo[key] = { matchedVersion: overridesKeys.includes(key) ? overrides[key] : remotes[key] });
      const { remoteEntry: { path: remoteEntryPath, name: remoteEntryName, type: remoteEntryType }, types: remoteTypes = {
        path: "",
        name: "",
        zip: "",
        api: ""
      }, buildInfo: { buildVersion }, globalName, ssrRemoteEntry } = manifest.metaData;
      const { exposes } = manifest;
      let basicRemoteSnapshot = {
        version: version ? version : "",
        buildVersion,
        globalName,
        remoteEntry: simpleJoinRemoteEntry(remoteEntryPath, remoteEntryName),
        remoteEntryType,
        remoteTypes: simpleJoinRemoteEntry(remoteTypes.path, remoteTypes.name),
        remoteTypesZip: remoteTypes.zip || "",
        remoteTypesAPI: remoteTypes.api || "",
        remotesInfo,
        shared: manifest == null ? void 0 : manifest.shared.map((item) => ({
          assets: item.assets,
          sharedName: item.name,
          version: item.version,
          usedExports: item.referenceExports || []
        })),
        modules: exposes == null ? void 0 : exposes.map((expose) => ({
          moduleName: expose.name,
          modulePath: expose.path,
          assets: expose.assets
        }))
      };
      if ((_b = manifest.metaData) == null ? void 0 : _b.prefetchInterface) {
        const prefetchInterface = manifest.metaData.prefetchInterface;
        basicRemoteSnapshot = {
          ...basicRemoteSnapshot,
          prefetchInterface
        };
      }
      if ((_c = manifest.metaData) == null ? void 0 : _c.prefetchEntry) {
        const { path: path2, name, type } = manifest.metaData.prefetchEntry;
        basicRemoteSnapshot = {
          ...basicRemoteSnapshot,
          prefetchEntry: simpleJoinRemoteEntry(path2, name),
          prefetchEntryType: type
        };
      }
      if ("publicPath" in manifest.metaData) {
        remoteSnapshot = {
          ...basicRemoteSnapshot,
          publicPath: getPublicPath()
        };
        if (typeof manifest.metaData.ssrPublicPath === "string") remoteSnapshot.ssrPublicPath = manifest.metaData.ssrPublicPath;
      } else remoteSnapshot = {
        ...basicRemoteSnapshot,
        getPublicPath: getPublicPath()
      };
      if (ssrRemoteEntry) {
        const fullSSRRemoteEntry = simpleJoinRemoteEntry(ssrRemoteEntry.path, ssrRemoteEntry.name);
        remoteSnapshot.ssrRemoteEntry = fullSSRRemoteEntry;
        remoteSnapshot.ssrRemoteEntryType = ssrRemoteEntry.type || "commonjs-module";
      }
      return remoteSnapshot;
    }
    function isManifestProvider(moduleInfo) {
      if ("remoteEntry" in moduleInfo && moduleInfo.remoteEntry.includes(require_constant3.MANIFEST_EXT)) return true;
      else return false;
    }
    function getManifestFileName(manifestOptions) {
      if (!manifestOptions) return {
        statsFileName: require_constant3.StatsFileName,
        manifestFileName: require_constant3.ManifestFileName
      };
      let filePath = typeof manifestOptions === "boolean" ? "" : manifestOptions.filePath || "";
      let fileName = typeof manifestOptions === "boolean" ? "" : manifestOptions.fileName || "";
      const JSON_EXT = ".json";
      const addExt = (name) => {
        if (name.endsWith(JSON_EXT)) return name;
        return `${name}${JSON_EXT}`;
      };
      const insertSuffix = (name, suffix) => {
        return name.replace(JSON_EXT, `${suffix}${JSON_EXT}`);
      };
      const manifestFileName = fileName ? addExt(fileName) : require_constant3.ManifestFileName;
      return {
        statsFileName: simpleJoinRemoteEntry(filePath, fileName ? insertSuffix(manifestFileName, "-stats") : require_constant3.StatsFileName),
        manifestFileName: simpleJoinRemoteEntry(filePath, manifestFileName)
      };
    }
    exports2.generateSnapshotFromManifest = generateSnapshotFromManifest;
    exports2.getManifestFileName = getManifestFileName;
    exports2.inferAutoPublicPath = inferAutoPublicPath;
    exports2.isManifestProvider = isManifestProvider;
    exports2.simpleJoinRemoteEntry = simpleJoinRemoteEntry;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/logger.cjs
var require_logger = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/logger.cjs"(exports2) {
    var require_env3 = require_env();
    var PREFIX = "[ Module Federation ]";
    var DEFAULT_DELEGATE = console;
    var LOGGER_STACK_SKIP_TOKENS = [
      "logger.ts",
      "logger.js",
      "captureStackTrace",
      "Logger.emit",
      "Logger.log",
      "Logger.info",
      "Logger.warn",
      "Logger.error",
      "Logger.debug"
    ];
    function captureStackTrace() {
      try {
        const stack = new Error().stack;
        if (!stack) return;
        const [, ...rawLines] = stack.split("\n");
        const filtered = rawLines.filter((line) => !LOGGER_STACK_SKIP_TOKENS.some((token) => line.includes(token)));
        if (!filtered.length) return;
        return `Stack trace:
${filtered.slice(0, 5).join("\n")}`;
      } catch {
        return;
      }
    }
    var Logger = class {
      constructor(prefix, delegate = DEFAULT_DELEGATE) {
        this.prefix = prefix;
        this.delegate = delegate ?? DEFAULT_DELEGATE;
      }
      setPrefix(prefix) {
        this.prefix = prefix;
      }
      setDelegate(delegate) {
        this.delegate = delegate ?? DEFAULT_DELEGATE;
      }
      emit(method, args) {
        const delegate = this.delegate;
        const stackTrace = require_env3.isDebugMode() ? captureStackTrace() : void 0;
        const enrichedArgs = stackTrace ? [...args, stackTrace] : args;
        const order = (() => {
          switch (method) {
            case "log":
              return ["log", "info"];
            case "info":
              return ["info", "log"];
            case "warn":
              return [
                "warn",
                "info",
                "log"
              ];
            case "error":
              return [
                "error",
                "warn",
                "log"
              ];
            default:
              return ["debug", "log"];
          }
        })();
        for (const candidate of order) {
          const handler = delegate[candidate];
          if (typeof handler === "function") {
            handler.call(delegate, this.prefix, ...enrichedArgs);
            return;
          }
        }
        for (const candidate of order) {
          const handler = DEFAULT_DELEGATE[candidate];
          if (typeof handler === "function") {
            handler.call(DEFAULT_DELEGATE, this.prefix, ...enrichedArgs);
            return;
          }
        }
      }
      log(...args) {
        this.emit("log", args);
      }
      warn(...args) {
        this.emit("warn", args);
      }
      error(...args) {
        this.emit("error", args);
      }
      success(...args) {
        this.emit("info", args);
      }
      info(...args) {
        this.emit("info", args);
      }
      ready(...args) {
        this.emit("info", args);
      }
      debug(...args) {
        if (require_env3.isDebugMode()) this.emit("debug", args);
      }
    };
    function createLogger(prefix) {
      return new Logger(prefix);
    }
    function createInfrastructureLogger(prefix) {
      const infrastructureLogger2 = new Logger(prefix);
      Object.defineProperty(infrastructureLogger2, "__mf_infrastructure_logger__", {
        value: true,
        enumerable: false,
        configurable: false
      });
      return infrastructureLogger2;
    }
    function bindLoggerToCompiler(loggerInstance, compiler, name) {
      if (!loggerInstance.__mf_infrastructure_logger__) return;
      if (!(compiler == null ? void 0 : compiler.getInfrastructureLogger)) return;
      try {
        const infrastructureLogger2 = compiler.getInfrastructureLogger(name);
        if (infrastructureLogger2 && typeof infrastructureLogger2 === "object" && (typeof infrastructureLogger2.log === "function" || typeof infrastructureLogger2.info === "function" || typeof infrastructureLogger2.warn === "function" || typeof infrastructureLogger2.error === "function")) loggerInstance.setDelegate(infrastructureLogger2);
      } catch {
        loggerInstance.setDelegate(void 0);
      }
    }
    var logger = createLogger(PREFIX);
    var infrastructureLogger = createInfrastructureLogger(PREFIX);
    exports2.bindLoggerToCompiler = bindLoggerToCompiler;
    exports2.createInfrastructureLogger = createInfrastructureLogger;
    exports2.createLogger = createLogger;
    exports2.infrastructureLogger = infrastructureLogger;
    exports2.logger = logger;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/dom.cjs
var require_dom = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/dom.cjs"(exports2) {
    var require_utils5 = require_utils();
    async function safeWrapper(callback, disableWarn) {
      try {
        return await callback();
      } catch (e) {
        !disableWarn && require_utils5.warn(e);
        return;
      }
    }
    function isStaticResourcesEqual(url1, url2) {
      const REG_EXP = /^(https?:)?\/\//i;
      return url1.replace(REG_EXP, "").replace(/\/$/, "") === url2.replace(REG_EXP, "").replace(/\/$/, "");
    }
    function createScript(info) {
      let script2 = null;
      let needAttach = true;
      let timeout = 2e4;
      let timeoutId;
      const scripts = document.getElementsByTagName("script");
      for (let i = 0; i < scripts.length; i++) {
        const s = scripts[i];
        const scriptSrc = s.getAttribute("src");
        if (scriptSrc && isStaticResourcesEqual(scriptSrc, info.url)) {
          script2 = s;
          needAttach = false;
          break;
        }
      }
      if (!script2) {
        const attrs2 = info.attrs;
        script2 = document.createElement("script");
        script2.type = (attrs2 == null ? void 0 : attrs2["type"]) === "module" ? "module" : "text/javascript";
        let createScriptRes = void 0;
        if (info.createScriptHook) {
          createScriptRes = info.createScriptHook(info.url, info.attrs);
          if (createScriptRes instanceof HTMLScriptElement) script2 = createScriptRes;
          else if (typeof createScriptRes === "object") {
            if ("script" in createScriptRes && createScriptRes.script) script2 = createScriptRes.script;
            if ("timeout" in createScriptRes && createScriptRes.timeout) timeout = createScriptRes.timeout;
          }
        }
        if (!script2.src) script2.src = info.url;
        if (attrs2 && !createScriptRes) Object.keys(attrs2).forEach((name) => {
          if (script2) {
            if (name === "async" || name === "defer") script2[name] = attrs2[name];
            else if (!script2.getAttribute(name)) script2.setAttribute(name, attrs2[name]);
          }
        });
      }
      let executionError = null;
      const executionErrorHandler = typeof window !== "undefined" ? (evt) => {
        if (evt.filename && isStaticResourcesEqual(evt.filename, info.url)) {
          const err = new Error(`ScriptExecutionError: Script "${info.url}" loaded but threw a runtime error during execution: ${evt.message} (${evt.filename}:${evt.lineno}:${evt.colno})`);
          err.name = "ScriptExecutionError";
          executionError = err;
        }
      } : null;
      if (executionErrorHandler) window.addEventListener("error", executionErrorHandler);
      const onScriptComplete = async (prev, event) => {
        clearTimeout(timeoutId);
        if (executionErrorHandler) window.removeEventListener("error", executionErrorHandler);
        const onScriptCompleteCallback = () => {
          if ((event == null ? void 0 : event.type) === "error") {
            const networkError = new Error(`ScriptNetworkError: Failed to load script "${info.url}" - the script URL is unreachable or the server returned an error (network failure, 404, CORS, etc.)`);
            networkError.name = "ScriptNetworkError";
            (info == null ? void 0 : info.onErrorCallback) && (info == null ? void 0 : info.onErrorCallback(networkError));
          } else if (executionError) (info == null ? void 0 : info.onErrorCallback) && (info == null ? void 0 : info.onErrorCallback(executionError));
          else (info == null ? void 0 : info.cb) && (info == null ? void 0 : info.cb());
        };
        if (script2) {
          script2.onerror = null;
          script2.onload = null;
          safeWrapper(() => {
            const { needDeleteScript = true } = info;
            if (needDeleteScript) (script2 == null ? void 0 : script2.parentNode) && script2.parentNode.removeChild(script2);
          });
          if (prev && typeof prev === "function") {
            const result = prev(event);
            if (result instanceof Promise) {
              const res2 = await result;
              onScriptCompleteCallback();
              return res2;
            }
            onScriptCompleteCallback();
            return result;
          }
        }
        onScriptCompleteCallback();
      };
      script2.onerror = onScriptComplete.bind(null, script2.onerror);
      script2.onload = onScriptComplete.bind(null, script2.onload);
      timeoutId = setTimeout(() => {
        onScriptComplete(null, new Error(`Remote script "${info.url}" time-outed.`));
      }, timeout);
      return {
        script: script2,
        needAttach
      };
    }
    function createLink(info) {
      let link = null;
      let needAttach = true;
      const links = document.getElementsByTagName("link");
      for (let i = 0; i < links.length; i++) {
        const l = links[i];
        const linkHref = l.getAttribute("href");
        const linkRel = l.getAttribute("rel");
        if (linkHref && isStaticResourcesEqual(linkHref, info.url) && linkRel === info.attrs["rel"]) {
          link = l;
          needAttach = false;
          break;
        }
      }
      if (!link) {
        link = document.createElement("link");
        link.setAttribute("href", info.url);
        let createLinkRes = void 0;
        const attrs2 = info.attrs;
        if (info.createLinkHook) {
          createLinkRes = info.createLinkHook(info.url, attrs2);
          if (createLinkRes instanceof HTMLLinkElement) link = createLinkRes;
        }
        if (attrs2 && !createLinkRes) Object.keys(attrs2).forEach((name) => {
          if (link && !link.getAttribute(name)) link.setAttribute(name, attrs2[name]);
        });
      }
      const onLinkComplete = (prev, event) => {
        const onLinkCompleteCallback = () => {
          if ((event == null ? void 0 : event.type) === "error") (info == null ? void 0 : info.onErrorCallback) && (info == null ? void 0 : info.onErrorCallback(event));
          else (info == null ? void 0 : info.cb) && (info == null ? void 0 : info.cb());
        };
        if (link) {
          link.onerror = null;
          link.onload = null;
          safeWrapper(() => {
            const { needDeleteLink = true } = info;
            if (needDeleteLink) (link == null ? void 0 : link.parentNode) && link.parentNode.removeChild(link);
          });
          if (prev) {
            const res2 = prev(event);
            onLinkCompleteCallback();
            return res2;
          }
        }
        onLinkCompleteCallback();
      };
      link.onerror = onLinkComplete.bind(null, link.onerror);
      link.onload = onLinkComplete.bind(null, link.onload);
      return {
        link,
        needAttach
      };
    }
    function loadScript(url2, info) {
      const { attrs: attrs2 = {}, createScriptHook } = info;
      return new Promise((resolve, reject) => {
        const { script: script2, needAttach } = createScript({
          url: url2,
          cb: resolve,
          onErrorCallback: reject,
          attrs: {
            fetchpriority: "high",
            ...attrs2
          },
          createScriptHook,
          needDeleteScript: true
        });
        needAttach && document.head.appendChild(script2);
      });
    }
    exports2.createLink = createLink;
    exports2.createScript = createScript;
    exports2.isStaticResourcesEqual = isStaticResourcesEqual;
    exports2.loadScript = loadScript;
    exports2.safeWrapper = safeWrapper;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/node.cjs
var require_node = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/node.cjs"(exports, module) {
    var sdkImportCache = /* @__PURE__ */ new Map();
    function importNodeModule(name) {
      if (!name) throw new Error("import specifier is required");
      if (sdkImportCache.has(name)) return sdkImportCache.get(name);
      const promise = new Function("name", `return import(name)`)(name).then((res2) => res2).catch((error) => {
        console.error(`Error importing module ${name}:`, error);
        sdkImportCache.delete(name);
        throw error;
      });
      sdkImportCache.set(name, promise);
      return promise;
    }
    var loadNodeFetch = async () => {
      const fetchModule = await importNodeModule("node-fetch");
      return fetchModule.default || fetchModule;
    };
    var lazyLoaderHookFetch = async (input, init, loaderHook2) => {
      const hook = (url2, init2) => {
        return loaderHook2.lifecycle.fetch.emit(url2, init2);
      };
      const res2 = await hook(input, init || {});
      if (!res2 || !(res2 instanceof Response)) return (typeof fetch === "undefined" ? await loadNodeFetch() : fetch)(input, init || {});
      return res2;
    };
    var createScriptNode = typeof ENV_TARGET === "undefined" || ENV_TARGET !== "web" ? (url, cb, attrs, loaderHook) => {
      if (loaderHook == null ? void 0 : loaderHook.createScriptHook) {
        const hookResult = loaderHook.createScriptHook(url);
        if (hookResult && typeof hookResult === "object" && "url" in hookResult) url = hookResult.url;
      }
      let urlObj;
      try {
        urlObj = new URL(url);
      } catch (e) {
        console.error("Error constructing URL:", e);
        cb(new Error(`Invalid URL: ${e}`));
        return;
      }
      const getFetch = async () => {
        if (loaderHook == null ? void 0 : loaderHook.fetch) return (input, init) => lazyLoaderHookFetch(input, init, loaderHook);
        return typeof fetch === "undefined" ? loadNodeFetch() : fetch;
      };
      const handleScriptFetch = async (f, urlObj) => {
        var _a;
        try {
          const res = await f(urlObj.href);
          const data = await res.text();
          const [path, vm] = await Promise.all([importNodeModule("path"), importNodeModule("vm")]);
          const scriptContext = {
            exports: {},
            module: { exports: {} }
          };
          const urlDirname = urlObj.pathname.split("/").slice(0, -1).join("/");
          const filename = path.basename(urlObj.pathname);
          const script = new vm.Script(`(function(exports, module, require, __dirname, __filename) {${data}
})`, {
            filename,
            importModuleDynamically: ((_a = vm.constants) == null ? void 0 : _a.USE_MAIN_CONTEXT_DEFAULT_LOADER) ?? importNodeModule
          });
          let requireFn;
          requireFn = eval("require");
          script.runInThisContext()(scriptContext.exports, scriptContext.module, requireFn, urlDirname, filename);
          const exportedInterface = scriptContext.module.exports || scriptContext.exports;
          if (attrs && exportedInterface && attrs["globalName"]) {
            cb(void 0, exportedInterface[attrs["globalName"]] || exportedInterface);
            return;
          }
          cb(void 0, exportedInterface);
        } catch (e) {
          cb(e instanceof Error ? e : new Error(`Script execution error: ${e}`));
        }
      };
      getFetch().then(async (f2) => {
        if ((attrs == null ? void 0 : attrs["type"]) === "esm" || (attrs == null ? void 0 : attrs["type"]) === "module") return loadModule(urlObj.href, {
          fetch: f2,
          vm: await importNodeModule("vm")
        }).then(async (module2) => {
          await module2.evaluate();
          cb(void 0, module2.namespace);
        }).catch((e) => {
          cb(e instanceof Error ? e : new Error(`Script execution error: ${e}`));
        });
        handleScriptFetch(f2, urlObj);
      }).catch((err) => {
        cb(err);
      });
    } : (url2, cb2, attrs2, loaderHook2) => {
      cb2(new Error("createScriptNode is disabled in non-Node.js environment"));
    };
    var loadScriptNode = typeof ENV_TARGET === "undefined" || ENV_TARGET !== "web" ? (url2, info) => {
      return new Promise((resolve, reject) => {
        createScriptNode(url2, (error, scriptContext2) => {
          var _a, _b;
          if (error) reject(error);
          else {
            const remoteEntryKey = ((_a = info == null ? void 0 : info.attrs) == null ? void 0 : _a["globalName"]) || `__FEDERATION_${(_b = info == null ? void 0 : info.attrs) == null ? void 0 : _b["name"]}:custom__`;
            resolve(globalThis[remoteEntryKey] = scriptContext2);
          }
        }, info.attrs, info.loaderHook);
      });
    } : (url2, info) => {
      throw new Error("loadScriptNode is disabled in non-Node.js environment");
    };
    var esmModuleCache = /* @__PURE__ */ new Map();
    async function loadModule(url2, options) {
      if (esmModuleCache.has(url2)) return esmModuleCache.get(url2);
      const { fetch: fetch2, vm: vm2 } = options;
      const code = await (await fetch2(url2)).text();
      const module2 = new vm2.SourceTextModule(code, { importModuleDynamically: async (specifier, script2) => {
        const resolvedUrl = new URL(specifier, url2).href;
        return loadModule(resolvedUrl, options);
      } });
      esmModuleCache.set(url2, module2);
      await module2.link(async (specifier) => {
        const resolvedUrl = new URL(specifier, url2).href;
        return await loadModule(resolvedUrl, options);
      });
      return module2;
    }
    exports.createScriptNode = createScriptNode;
    exports.loadScriptNode = loadScriptNode;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/normalizeOptions.cjs
var require_normalizeOptions = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/normalizeOptions.cjs"(exports2) {
    function normalizeOptions(enableDefault, defaultOptions, key) {
      return function(options) {
        if (options === false) return false;
        if (typeof options === "undefined") if (enableDefault) return defaultOptions;
        else return false;
        if (options === true) return defaultOptions;
        if (options && typeof options === "object") return {
          ...defaultOptions,
          ...options
        };
        throw new Error(`Unexpected type for \`${key}\`, expect boolean/undefined/object, got: ${typeof options}`);
      };
    }
    exports2.normalizeOptions = normalizeOptions;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/createModuleFederationConfig.cjs
var require_createModuleFederationConfig = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/createModuleFederationConfig.cjs"(exports2) {
    var createModuleFederationConfig = (options) => {
      return options;
    };
    exports2.createModuleFederationConfig = createModuleFederationConfig;
  }
});

// node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/index.cjs
var require_dist = __commonJS({
  "node_modules/.pnpm/@module-federation+sdk@2.2.2/node_modules/@module-federation/sdk/dist/index.cjs"(exports2) {
    Object.defineProperty(exports2, Symbol.toStringTag, { value: "Module" });
    var require_constant3 = require_constant();
    var require_ContainerPlugin2 = require_ContainerPlugin();
    var require_ContainerReferencePlugin2 = require_ContainerReferencePlugin();
    var require_ModuleFederationPlugin2 = require_ModuleFederationPlugin();
    var require_SharePlugin2 = require_SharePlugin();
    var require_ConsumeSharedPlugin2 = require_ConsumeSharedPlugin();
    var require_ProvideSharedPlugin2 = require_ProvideSharedPlugin();
    var require_env3 = require_env();
    var require_utils5 = require_utils();
    var require_generateSnapshotFromManifest2 = require_generateSnapshotFromManifest();
    var require_logger3 = require_logger();
    var require_dom2 = require_dom();
    var require_node2 = require_node();
    var require_normalizeOptions2 = require_normalizeOptions();
    var require_createModuleFederationConfig2 = require_createModuleFederationConfig();
    exports2.BROWSER_LOG_KEY = require_constant3.BROWSER_LOG_KEY;
    exports2.ENCODE_NAME_PREFIX = require_constant3.ENCODE_NAME_PREFIX;
    exports2.EncodedNameTransformMap = require_constant3.EncodedNameTransformMap;
    exports2.FederationModuleManifest = require_constant3.FederationModuleManifest;
    exports2.MANIFEST_EXT = require_constant3.MANIFEST_EXT;
    exports2.MFModuleType = require_constant3.MFModuleType;
    exports2.MFPrefetchCommon = require_constant3.MFPrefetchCommon;
    exports2.MODULE_DEVTOOL_IDENTIFIER = require_constant3.MODULE_DEVTOOL_IDENTIFIER;
    exports2.ManifestFileName = require_constant3.ManifestFileName;
    exports2.NameTransformMap = require_constant3.NameTransformMap;
    exports2.NameTransformSymbol = require_constant3.NameTransformSymbol;
    exports2.SEPARATOR = require_constant3.SEPARATOR;
    exports2.StatsFileName = require_constant3.StatsFileName;
    exports2.TEMP_DIR = require_constant3.TEMP_DIR;
    exports2.TreeShakingStatus = require_constant3.TreeShakingStatus;
    exports2.assert = require_utils5.assert;
    exports2.bindLoggerToCompiler = require_logger3.bindLoggerToCompiler;
    exports2.composeKeyWithSeparator = require_utils5.composeKeyWithSeparator;
    Object.defineProperty(exports2, "consumeSharedPlugin", {
      enumerable: true,
      get: function() {
        return require_ConsumeSharedPlugin2.ConsumeSharedPlugin_exports;
      }
    });
    Object.defineProperty(exports2, "containerPlugin", {
      enumerable: true,
      get: function() {
        return require_ContainerPlugin2.ContainerPlugin_exports;
      }
    });
    Object.defineProperty(exports2, "containerReferencePlugin", {
      enumerable: true,
      get: function() {
        return require_ContainerReferencePlugin2.ContainerReferencePlugin_exports;
      }
    });
    exports2.createInfrastructureLogger = require_logger3.createInfrastructureLogger;
    exports2.createLink = require_dom2.createLink;
    exports2.createLogger = require_logger3.createLogger;
    exports2.createModuleFederationConfig = require_createModuleFederationConfig2.createModuleFederationConfig;
    exports2.createScript = require_dom2.createScript;
    exports2.createScriptNode = require_node2.createScriptNode;
    exports2.decodeName = require_utils5.decodeName;
    exports2.encodeName = require_utils5.encodeName;
    exports2.error = require_utils5.error;
    exports2.generateExposeFilename = require_utils5.generateExposeFilename;
    exports2.generateShareFilename = require_utils5.generateShareFilename;
    exports2.generateSnapshotFromManifest = require_generateSnapshotFromManifest2.generateSnapshotFromManifest;
    exports2.getManifestFileName = require_generateSnapshotFromManifest2.getManifestFileName;
    exports2.getProcessEnv = require_env3.getProcessEnv;
    exports2.getResourceUrl = require_utils5.getResourceUrl;
    exports2.inferAutoPublicPath = require_generateSnapshotFromManifest2.inferAutoPublicPath;
    exports2.infrastructureLogger = require_logger3.infrastructureLogger;
    exports2.isBrowserEnv = require_env3.isBrowserEnv;
    exports2.isBrowserEnvValue = require_env3.isBrowserEnvValue;
    exports2.isDebugMode = require_env3.isDebugMode;
    exports2.isManifestProvider = require_generateSnapshotFromManifest2.isManifestProvider;
    exports2.isReactNativeEnv = require_env3.isReactNativeEnv;
    exports2.isRequiredVersion = require_utils5.isRequiredVersion;
    exports2.isStaticResourcesEqual = require_dom2.isStaticResourcesEqual;
    exports2.loadScript = require_dom2.loadScript;
    exports2.loadScriptNode = require_node2.loadScriptNode;
    exports2.logger = require_logger3.logger;
    Object.defineProperty(exports2, "moduleFederationPlugin", {
      enumerable: true,
      get: function() {
        return require_ModuleFederationPlugin2.ModuleFederationPlugin_exports;
      }
    });
    exports2.normalizeOptions = require_normalizeOptions2.normalizeOptions;
    exports2.parseEntry = require_utils5.parseEntry;
    Object.defineProperty(exports2, "provideSharedPlugin", {
      enumerable: true,
      get: function() {
        return require_ProvideSharedPlugin2.ProvideSharedPlugin_exports;
      }
    });
    exports2.safeToString = require_utils5.safeToString;
    exports2.safeWrapper = require_dom2.safeWrapper;
    Object.defineProperty(exports2, "sharePlugin", {
      enumerable: true,
      get: function() {
        return require_SharePlugin2.SharePlugin_exports;
      }
    });
    exports2.simpleJoinRemoteEntry = require_generateSnapshotFromManifest2.simpleJoinRemoteEntry;
    exports2.warn = require_utils5.warn;
  }
});

// node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/getShortErrorMsg.cjs
var require_getShortErrorMsg = __commonJS({
  "node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/getShortErrorMsg.cjs"(exports2) {
    var getDocsUrl = (errorCode) => {
      return `View the docs to see how to solve: https://module-federation.io/guide/troubleshooting/${errorCode.split("-")[0].toLowerCase()}#${errorCode.toLowerCase()}`;
    };
    var getShortErrorMsg = (errorCode, errorDescMap, args, originalErrorMsg) => {
      const msg = [`${[errorDescMap[errorCode]]} #${errorCode}`];
      args && msg.push(`args: ${JSON.stringify(args)}`);
      msg.push(getDocsUrl(errorCode));
      originalErrorMsg && msg.push(`Original Error Message:
 ${originalErrorMsg}`);
      return msg.join("\n");
    };
    exports2.getShortErrorMsg = getShortErrorMsg;
  }
});

// node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/browser.cjs
var require_browser = __commonJS({
  "node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/browser.cjs"(exports2) {
    Object.defineProperty(exports2, Symbol.toStringTag, { value: "Module" });
    var require_getShortErrorMsg2 = require_getShortErrorMsg();
    function logAndReport(code, descMap, args, logger, originalErrorMsg, context) {
      return logger(require_getShortErrorMsg2.getShortErrorMsg(code, descMap, args, originalErrorMsg));
    }
    exports2.logAndReport = logAndReport;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/logger.cjs
var require_logger2 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/logger.cjs"(exports2) {
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes_browser = require_browser();
    var LOG_CATEGORY = "[ Federation Runtime ]";
    var logger = (0, _module_federation_sdk.createLogger)(LOG_CATEGORY);
    function assert(condition, msgOrCode, descMap, args, context) {
      if (!condition) if (descMap !== void 0) error(msgOrCode, descMap, args, void 0, context);
      else error(msgOrCode);
    }
    function error(msgOrCode, descMap, args, originalErrorMsg, context) {
      if (descMap !== void 0) return (0, _module_federation_error_codes_browser.logAndReport)(msgOrCode, descMap, args ?? {}, (msg2) => {
        throw new Error(`${LOG_CATEGORY}: ${msg2}`);
      }, originalErrorMsg, context);
      const msg = msgOrCode;
      if (msg instanceof Error) {
        if (!msg.message.startsWith(LOG_CATEGORY)) msg.message = `${LOG_CATEGORY}: ${msg.message}`;
        throw msg;
      }
      throw new Error(`${LOG_CATEGORY}: ${msg}`);
    }
    function warn(msg) {
      if (msg instanceof Error) {
        if (!msg.message.startsWith(LOG_CATEGORY)) msg.message = `${LOG_CATEGORY}: ${msg.message}`;
        logger.warn(msg);
      } else logger.warn(msg);
    }
    exports2.assert = assert;
    exports2.error = error;
    exports2.logger = logger;
    exports2.warn = warn;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/tool.cjs
var require_tool = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/tool.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var _module_federation_sdk = require_dist();
    function addUniqueItem(arr, item) {
      if (arr.findIndex((name) => name === item) === -1) arr.push(item);
      return arr;
    }
    function getFMId(remoteInfo) {
      if ("version" in remoteInfo && remoteInfo.version) return `${remoteInfo.name}:${remoteInfo.version}`;
      else if ("entry" in remoteInfo && remoteInfo.entry) return `${remoteInfo.name}:${remoteInfo.entry}`;
      else return `${remoteInfo.name}`;
    }
    function isRemoteInfoWithEntry(remote) {
      return typeof remote.entry !== "undefined";
    }
    function isPureRemoteEntry(remote) {
      return !remote.entry.includes(".json");
    }
    async function safeWrapper(callback, disableWarn) {
      try {
        return await callback();
      } catch (e) {
        !disableWarn && require_logger3.warn(e);
        return;
      }
    }
    function isObject(val) {
      return val && typeof val === "object";
    }
    var objectToString = Object.prototype.toString;
    function isPlainObject(val) {
      return objectToString.call(val) === "[object Object]";
    }
    function isStaticResourcesEqual(url1, url2) {
      const REG_EXP = /^(https?:)?\/\//i;
      return url1.replace(REG_EXP, "").replace(/\/$/, "") === url2.replace(REG_EXP, "").replace(/\/$/, "");
    }
    function arrayOptions(options) {
      return Array.isArray(options) ? options : [options];
    }
    function getRemoteEntryInfoFromSnapshot(snapshot) {
      const defaultRemoteEntryInfo = {
        url: "",
        type: "global",
        globalName: ""
      };
      if (_module_federation_sdk.isBrowserEnvValue || (0, _module_federation_sdk.isReactNativeEnv)() || !("ssrRemoteEntry" in snapshot)) return "remoteEntry" in snapshot ? {
        url: snapshot.remoteEntry,
        type: snapshot.remoteEntryType,
        globalName: snapshot.globalName
      } : defaultRemoteEntryInfo;
      if ("ssrRemoteEntry" in snapshot) return {
        url: snapshot.ssrRemoteEntry || defaultRemoteEntryInfo.url,
        type: snapshot.ssrRemoteEntryType || defaultRemoteEntryInfo.type,
        globalName: snapshot.globalName
      };
      return defaultRemoteEntryInfo;
    }
    var processModuleAlias = (name, subPath) => {
      let moduleName;
      if (name.endsWith("/")) moduleName = name.slice(0, -1);
      else moduleName = name;
      if (subPath.startsWith(".")) subPath = subPath.slice(1);
      moduleName = moduleName + subPath;
      return moduleName;
    };
    exports2.addUniqueItem = addUniqueItem;
    exports2.arrayOptions = arrayOptions;
    exports2.getFMId = getFMId;
    exports2.getRemoteEntryInfoFromSnapshot = getRemoteEntryInfoFromSnapshot;
    exports2.isObject = isObject;
    exports2.isPlainObject = isPlainObject;
    exports2.isPureRemoteEntry = isPureRemoteEntry;
    exports2.isRemoteInfoWithEntry = isRemoteInfoWithEntry;
    exports2.isStaticResourcesEqual = isStaticResourcesEqual;
    exports2.objectToString = objectToString;
    exports2.processModuleAlias = processModuleAlias;
    exports2.safeWrapper = safeWrapper;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/global.cjs
var require_global = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/global.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var _module_federation_sdk = require_dist();
    var CurrentGlobal = typeof globalThis === "object" ? globalThis : window;
    var nativeGlobal = (() => {
      try {
        return document.defaultView;
      } catch {
        return CurrentGlobal;
      }
    })();
    var Global = nativeGlobal;
    function definePropertyGlobalVal(target, key, val) {
      Object.defineProperty(target, key, {
        value: val,
        configurable: false,
        writable: true
      });
    }
    function includeOwnProperty(target, key) {
      return Object.hasOwnProperty.call(target, key);
    }
    if (!includeOwnProperty(CurrentGlobal, "__GLOBAL_LOADING_REMOTE_ENTRY__")) definePropertyGlobalVal(CurrentGlobal, "__GLOBAL_LOADING_REMOTE_ENTRY__", {});
    var globalLoading = CurrentGlobal.__GLOBAL_LOADING_REMOTE_ENTRY__;
    function setGlobalDefaultVal(target) {
      var _a, _b, _c, _d, _e, _f;
      if (includeOwnProperty(target, "__VMOK__") && !includeOwnProperty(target, "__FEDERATION__")) definePropertyGlobalVal(target, "__FEDERATION__", target.__VMOK__);
      if (!includeOwnProperty(target, "__FEDERATION__")) {
        definePropertyGlobalVal(target, "__FEDERATION__", {
          __GLOBAL_PLUGIN__: [],
          __INSTANCES__: [],
          moduleInfo: {},
          __SHARE__: {},
          __MANIFEST_LOADING__: {},
          __PRELOADED_MAP__: /* @__PURE__ */ new Map()
        });
        definePropertyGlobalVal(target, "__VMOK__", target.__FEDERATION__);
      }
      (_a = target.__FEDERATION__).__GLOBAL_PLUGIN__ ?? (_a.__GLOBAL_PLUGIN__ = []);
      (_b = target.__FEDERATION__).__INSTANCES__ ?? (_b.__INSTANCES__ = []);
      (_c = target.__FEDERATION__).moduleInfo ?? (_c.moduleInfo = {});
      (_d = target.__FEDERATION__).__SHARE__ ?? (_d.__SHARE__ = {});
      (_e = target.__FEDERATION__).__MANIFEST_LOADING__ ?? (_e.__MANIFEST_LOADING__ = {});
      (_f = target.__FEDERATION__).__PRELOADED_MAP__ ?? (_f.__PRELOADED_MAP__ = /* @__PURE__ */ new Map());
    }
    setGlobalDefaultVal(CurrentGlobal);
    setGlobalDefaultVal(nativeGlobal);
    function resetFederationGlobalInfo() {
      CurrentGlobal.__FEDERATION__.__GLOBAL_PLUGIN__ = [];
      CurrentGlobal.__FEDERATION__.__INSTANCES__ = [];
      CurrentGlobal.__FEDERATION__.moduleInfo = {};
      CurrentGlobal.__FEDERATION__.__SHARE__ = {};
      CurrentGlobal.__FEDERATION__.__MANIFEST_LOADING__ = {};
      Object.keys(globalLoading).forEach((key) => {
        delete globalLoading[key];
      });
    }
    function setGlobalFederationInstance(FederationInstance) {
      CurrentGlobal.__FEDERATION__.__INSTANCES__.push(FederationInstance);
    }
    function getGlobalFederationConstructor() {
      return CurrentGlobal.__FEDERATION__.__DEBUG_CONSTRUCTOR__;
    }
    function setGlobalFederationConstructor(FederationConstructor, isDebug = (0, _module_federation_sdk.isDebugMode)()) {
      if (isDebug) {
        CurrentGlobal.__FEDERATION__.__DEBUG_CONSTRUCTOR__ = FederationConstructor;
        CurrentGlobal.__FEDERATION__.__DEBUG_CONSTRUCTOR_VERSION__ = "2.2.2";
      }
    }
    function getInfoWithoutType(target, key) {
      if (typeof key === "string") if (target[key]) return {
        value: target[key],
        key
      };
      else {
        const targetKeys = Object.keys(target);
        for (const targetKey of targetKeys) {
          const [targetTypeOrName, _] = targetKey.split(":");
          const nKey = `${targetTypeOrName}:${key}`;
          const typeWithKeyRes = target[nKey];
          if (typeWithKeyRes) return {
            value: typeWithKeyRes,
            key: nKey
          };
        }
        return {
          value: void 0,
          key
        };
      }
      else require_logger3.error(`getInfoWithoutType: "key" must be a string, got ${typeof key} (${JSON.stringify(key)}).`);
    }
    var getGlobalSnapshot = () => nativeGlobal.__FEDERATION__.moduleInfo;
    var getTargetSnapshotInfoByModuleInfo = (moduleInfo, snapshot) => {
      const getModuleInfo = getInfoWithoutType(snapshot, require_tool2.getFMId(moduleInfo)).value;
      if (getModuleInfo && !getModuleInfo.version && "version" in moduleInfo && moduleInfo["version"]) getModuleInfo.version = moduleInfo["version"];
      if (getModuleInfo) return getModuleInfo;
      if ("version" in moduleInfo && moduleInfo["version"]) {
        const { version, ...resModuleInfo } = moduleInfo;
        const moduleKeyWithoutVersion = require_tool2.getFMId(resModuleInfo);
        const getModuleInfoWithoutVersion = getInfoWithoutType(nativeGlobal.__FEDERATION__.moduleInfo, moduleKeyWithoutVersion).value;
        if ((getModuleInfoWithoutVersion == null ? void 0 : getModuleInfoWithoutVersion.version) === version) return getModuleInfoWithoutVersion;
      }
    };
    var getGlobalSnapshotInfoByModuleInfo = (moduleInfo) => getTargetSnapshotInfoByModuleInfo(moduleInfo, nativeGlobal.__FEDERATION__.moduleInfo);
    var setGlobalSnapshotInfoByModuleInfo = (remoteInfo, moduleDetailInfo) => {
      const moduleKey = require_tool2.getFMId(remoteInfo);
      nativeGlobal.__FEDERATION__.moduleInfo[moduleKey] = moduleDetailInfo;
      return nativeGlobal.__FEDERATION__.moduleInfo;
    };
    var addGlobalSnapshot = (moduleInfos) => {
      nativeGlobal.__FEDERATION__.moduleInfo = {
        ...nativeGlobal.__FEDERATION__.moduleInfo,
        ...moduleInfos
      };
      return () => {
        const keys = Object.keys(moduleInfos);
        for (const key of keys) delete nativeGlobal.__FEDERATION__.moduleInfo[key];
      };
    };
    var getRemoteEntryExports = (name, globalName) => {
      const remoteEntryKey = globalName || `__FEDERATION_${name}:custom__`;
      return {
        remoteEntryKey,
        entryExports: CurrentGlobal[remoteEntryKey]
      };
    };
    var registerGlobalPlugins = (plugins) => {
      const { __GLOBAL_PLUGIN__ } = nativeGlobal.__FEDERATION__;
      plugins.forEach((plugin) => {
        if (__GLOBAL_PLUGIN__.findIndex((p) => p.name === plugin.name) === -1) __GLOBAL_PLUGIN__.push(plugin);
        else require_logger3.warn(`The plugin ${plugin.name} has been registered.`);
      });
    };
    var getGlobalHostPlugins = () => nativeGlobal.__FEDERATION__.__GLOBAL_PLUGIN__;
    var getPreloaded = (id) => CurrentGlobal.__FEDERATION__.__PRELOADED_MAP__.get(id);
    var setPreloaded = (id) => CurrentGlobal.__FEDERATION__.__PRELOADED_MAP__.set(id, true);
    exports2.CurrentGlobal = CurrentGlobal;
    exports2.Global = Global;
    exports2.addGlobalSnapshot = addGlobalSnapshot;
    exports2.getGlobalFederationConstructor = getGlobalFederationConstructor;
    exports2.getGlobalHostPlugins = getGlobalHostPlugins;
    exports2.getGlobalSnapshot = getGlobalSnapshot;
    exports2.getGlobalSnapshotInfoByModuleInfo = getGlobalSnapshotInfoByModuleInfo;
    exports2.getInfoWithoutType = getInfoWithoutType;
    exports2.getPreloaded = getPreloaded;
    exports2.getRemoteEntryExports = getRemoteEntryExports;
    exports2.getTargetSnapshotInfoByModuleInfo = getTargetSnapshotInfoByModuleInfo;
    exports2.globalLoading = globalLoading;
    exports2.nativeGlobal = nativeGlobal;
    exports2.registerGlobalPlugins = registerGlobalPlugins;
    exports2.resetFederationGlobalInfo = resetFederationGlobalInfo;
    exports2.setGlobalFederationConstructor = setGlobalFederationConstructor;
    exports2.setGlobalFederationInstance = setGlobalFederationInstance;
    exports2.setGlobalSnapshotInfoByModuleInfo = setGlobalSnapshotInfoByModuleInfo;
    exports2.setPreloaded = setPreloaded;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/constants.cjs
var require_constants = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/constants.cjs"(exports2) {
    var buildIdentifier = "[0-9A-Za-z-]+";
    var build = `(?:\\+(${buildIdentifier}(?:\\.${buildIdentifier})*))`;
    var numericIdentifier = "0|[1-9]\\d*";
    var numericIdentifierLoose = "[0-9]+";
    var nonNumericIdentifier = "\\d*[a-zA-Z-][a-zA-Z0-9-]*";
    var preReleaseIdentifierLoose = `(?:${numericIdentifierLoose}|${nonNumericIdentifier})`;
    var preReleaseLoose = `(?:-?(${preReleaseIdentifierLoose}(?:\\.${preReleaseIdentifierLoose})*))`;
    var preReleaseIdentifier = `(?:${numericIdentifier}|${nonNumericIdentifier})`;
    var preRelease = `(?:-(${preReleaseIdentifier}(?:\\.${preReleaseIdentifier})*))`;
    var xRangeIdentifier = `${numericIdentifier}|x|X|\\*`;
    var xRangePlain = `[v=\\s]*(${xRangeIdentifier})(?:\\.(${xRangeIdentifier})(?:\\.(${xRangeIdentifier})(?:${preRelease})?${build}?)?)?`;
    var hyphenRange = `^\\s*(${xRangePlain})\\s+-\\s+(${xRangePlain})\\s*$`;
    var loosePlain = `[v=\\s]*${`(${numericIdentifierLoose})\\.(${numericIdentifierLoose})\\.(${numericIdentifierLoose})`}${preReleaseLoose}?${build}?`;
    var gtlt = "((?:<|>)?=?)";
    var comparatorTrim = `(\\s*)${gtlt}\\s*(${loosePlain}|${xRangePlain})`;
    var loneTilde = "(?:~>?)";
    var tildeTrim = `(\\s*)${loneTilde}\\s+`;
    var loneCaret = "(?:\\^)";
    var caretTrim = `(\\s*)${loneCaret}\\s+`;
    var star = "(<|>)?=?\\s*\\*";
    var caret = `^${loneCaret}${xRangePlain}$`;
    var fullPlain = `v?${`(${numericIdentifier})\\.(${numericIdentifier})\\.(${numericIdentifier})`}${preRelease}?${build}?`;
    var tilde = `^${loneTilde}${xRangePlain}$`;
    var xRange = `^${gtlt}\\s*${xRangePlain}$`;
    var comparator = `^${gtlt}\\s*(${fullPlain})$|^$`;
    var gte0 = "^\\s*>=\\s*0.0.0\\s*$";
    exports2.caret = caret;
    exports2.caretTrim = caretTrim;
    exports2.comparator = comparator;
    exports2.comparatorTrim = comparatorTrim;
    exports2.gte0 = gte0;
    exports2.hyphenRange = hyphenRange;
    exports2.star = star;
    exports2.tilde = tilde;
    exports2.tildeTrim = tildeTrim;
    exports2.xRange = xRange;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/utils.cjs
var require_utils2 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/utils.cjs"(exports2) {
    var require_constants2 = require_constants();
    function parseRegex(source) {
      return new RegExp(source);
    }
    function isXVersion(version) {
      return !version || version.toLowerCase() === "x" || version === "*";
    }
    function pipe(...fns) {
      return (x) => fns.reduce((v, f2) => f2(v), x);
    }
    function extractComparator(comparatorString) {
      return comparatorString.match(parseRegex(require_constants2.comparator));
    }
    function combineVersion(major, minor, patch, preRelease) {
      const mainVersion = `${major}.${minor}.${patch}`;
      if (preRelease) return `${mainVersion}-${preRelease}`;
      return mainVersion;
    }
    exports2.combineVersion = combineVersion;
    exports2.extractComparator = extractComparator;
    exports2.isXVersion = isXVersion;
    exports2.parseRegex = parseRegex;
    exports2.pipe = pipe;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/parser.cjs
var require_parser = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/parser.cjs"(exports2) {
    var require_constants2 = require_constants();
    var require_utils5 = require_utils2();
    function parseHyphen(range) {
      return range.replace(require_utils5.parseRegex(require_constants2.hyphenRange), (_range, from, fromMajor, fromMinor, fromPatch, _fromPreRelease, _fromBuild, to, toMajor, toMinor, toPatch, toPreRelease) => {
        if (require_utils5.isXVersion(fromMajor)) from = "";
        else if (require_utils5.isXVersion(fromMinor)) from = `>=${fromMajor}.0.0`;
        else if (require_utils5.isXVersion(fromPatch)) from = `>=${fromMajor}.${fromMinor}.0`;
        else from = `>=${from}`;
        if (require_utils5.isXVersion(toMajor)) to = "";
        else if (require_utils5.isXVersion(toMinor)) to = `<${Number(toMajor) + 1}.0.0-0`;
        else if (require_utils5.isXVersion(toPatch)) to = `<${toMajor}.${Number(toMinor) + 1}.0-0`;
        else if (toPreRelease) to = `<=${toMajor}.${toMinor}.${toPatch}-${toPreRelease}`;
        else to = `<=${to}`;
        return `${from} ${to}`.trim();
      });
    }
    function parseComparatorTrim(range) {
      return range.replace(require_utils5.parseRegex(require_constants2.comparatorTrim), "$1$2$3");
    }
    function parseTildeTrim(range) {
      return range.replace(require_utils5.parseRegex(require_constants2.tildeTrim), "$1~");
    }
    function parseCaretTrim(range) {
      return range.replace(require_utils5.parseRegex(require_constants2.caretTrim), "$1^");
    }
    function parseCarets(range) {
      return range.trim().split(/\s+/).map((rangeVersion) => rangeVersion.replace(require_utils5.parseRegex(require_constants2.caret), (_, major, minor, patch, preRelease) => {
        if (require_utils5.isXVersion(major)) return "";
        else if (require_utils5.isXVersion(minor)) return `>=${major}.0.0 <${Number(major) + 1}.0.0-0`;
        else if (require_utils5.isXVersion(patch)) if (major === "0") return `>=${major}.${minor}.0 <${major}.${Number(minor) + 1}.0-0`;
        else return `>=${major}.${minor}.0 <${Number(major) + 1}.0.0-0`;
        else if (preRelease) if (major === "0") if (minor === "0") return `>=${major}.${minor}.${patch}-${preRelease} <${major}.${minor}.${Number(patch) + 1}-0`;
        else return `>=${major}.${minor}.${patch}-${preRelease} <${major}.${Number(minor) + 1}.0-0`;
        else return `>=${major}.${minor}.${patch}-${preRelease} <${Number(major) + 1}.0.0-0`;
        else {
          if (major === "0") if (minor === "0") return `>=${major}.${minor}.${patch} <${major}.${minor}.${Number(patch) + 1}-0`;
          else return `>=${major}.${minor}.${patch} <${major}.${Number(minor) + 1}.0-0`;
          return `>=${major}.${minor}.${patch} <${Number(major) + 1}.0.0-0`;
        }
      })).join(" ");
    }
    function parseTildes(range) {
      return range.trim().split(/\s+/).map((rangeVersion) => rangeVersion.replace(require_utils5.parseRegex(require_constants2.tilde), (_, major, minor, patch, preRelease) => {
        if (require_utils5.isXVersion(major)) return "";
        else if (require_utils5.isXVersion(minor)) return `>=${major}.0.0 <${Number(major) + 1}.0.0-0`;
        else if (require_utils5.isXVersion(patch)) return `>=${major}.${minor}.0 <${major}.${Number(minor) + 1}.0-0`;
        else if (preRelease) return `>=${major}.${minor}.${patch}-${preRelease} <${major}.${Number(minor) + 1}.0-0`;
        return `>=${major}.${minor}.${patch} <${major}.${Number(minor) + 1}.0-0`;
      })).join(" ");
    }
    function parseXRanges(range) {
      return range.split(/\s+/).map((rangeVersion) => rangeVersion.trim().replace(require_utils5.parseRegex(require_constants2.xRange), (ret, gtlt, major, minor, patch, preRelease) => {
        const isXMajor = require_utils5.isXVersion(major);
        const isXMinor = isXMajor || require_utils5.isXVersion(minor);
        const isXPatch = isXMinor || require_utils5.isXVersion(patch);
        if (gtlt === "=" && isXPatch) gtlt = "";
        preRelease = "";
        if (isXMajor) if (gtlt === ">" || gtlt === "<") return "<0.0.0-0";
        else return "*";
        else if (gtlt && isXPatch) {
          if (isXMinor) minor = 0;
          patch = 0;
          if (gtlt === ">") {
            gtlt = ">=";
            if (isXMinor) {
              major = Number(major) + 1;
              minor = 0;
              patch = 0;
            } else {
              minor = Number(minor) + 1;
              patch = 0;
            }
          } else if (gtlt === "<=") {
            gtlt = "<";
            if (isXMinor) major = Number(major) + 1;
            else minor = Number(minor) + 1;
          }
          if (gtlt === "<") preRelease = "-0";
          return `${gtlt + major}.${minor}.${patch}${preRelease}`;
        } else if (isXMinor) return `>=${major}.0.0${preRelease} <${Number(major) + 1}.0.0-0`;
        else if (isXPatch) return `>=${major}.${minor}.0${preRelease} <${major}.${Number(minor) + 1}.0-0`;
        return ret;
      })).join(" ");
    }
    function parseStar(range) {
      return range.trim().replace(require_utils5.parseRegex(require_constants2.star), "");
    }
    function parseGTE0(comparatorString) {
      return comparatorString.trim().replace(require_utils5.parseRegex(require_constants2.gte0), "");
    }
    exports2.parseCaretTrim = parseCaretTrim;
    exports2.parseCarets = parseCarets;
    exports2.parseComparatorTrim = parseComparatorTrim;
    exports2.parseGTE0 = parseGTE0;
    exports2.parseHyphen = parseHyphen;
    exports2.parseStar = parseStar;
    exports2.parseTildeTrim = parseTildeTrim;
    exports2.parseTildes = parseTildes;
    exports2.parseXRanges = parseXRanges;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/compare.cjs
var require_compare = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/compare.cjs"(exports2) {
    function compareAtom(rangeAtom, versionAtom) {
      rangeAtom = Number(rangeAtom) || rangeAtom;
      versionAtom = Number(versionAtom) || versionAtom;
      if (rangeAtom > versionAtom) return 1;
      if (rangeAtom === versionAtom) return 0;
      return -1;
    }
    function comparePreRelease(rangeAtom, versionAtom) {
      const { preRelease: rangePreRelease } = rangeAtom;
      const { preRelease: versionPreRelease } = versionAtom;
      if (rangePreRelease === void 0 && Boolean(versionPreRelease)) return 1;
      if (Boolean(rangePreRelease) && versionPreRelease === void 0) return -1;
      if (rangePreRelease === void 0 && versionPreRelease === void 0) return 0;
      for (let i = 0, n = rangePreRelease.length; i <= n; i++) {
        const rangeElement = rangePreRelease[i];
        const versionElement = versionPreRelease[i];
        if (rangeElement === versionElement) continue;
        if (rangeElement === void 0 && versionElement === void 0) return 0;
        if (!rangeElement) return 1;
        if (!versionElement) return -1;
        return compareAtom(rangeElement, versionElement);
      }
      return 0;
    }
    function compareVersion(rangeAtom, versionAtom) {
      return compareAtom(rangeAtom.major, versionAtom.major) || compareAtom(rangeAtom.minor, versionAtom.minor) || compareAtom(rangeAtom.patch, versionAtom.patch) || comparePreRelease(rangeAtom, versionAtom);
    }
    function eq(rangeAtom, versionAtom) {
      return rangeAtom.version === versionAtom.version;
    }
    function compare(rangeAtom, versionAtom) {
      switch (rangeAtom.operator) {
        case "":
        case "=":
          return eq(rangeAtom, versionAtom);
        case ">":
          return compareVersion(rangeAtom, versionAtom) < 0;
        case ">=":
          return eq(rangeAtom, versionAtom) || compareVersion(rangeAtom, versionAtom) < 0;
        case "<":
          return compareVersion(rangeAtom, versionAtom) > 0;
        case "<=":
          return eq(rangeAtom, versionAtom) || compareVersion(rangeAtom, versionAtom) > 0;
        case void 0:
          return true;
        default:
          return false;
      }
    }
    exports2.compare = compare;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/index.cjs
var require_semver = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/semver/index.cjs"(exports2) {
    var require_utils5 = require_utils2();
    var require_parser2 = require_parser();
    var require_compare2 = require_compare();
    function parseComparatorString(range) {
      return require_utils5.pipe(require_parser2.parseCarets, require_parser2.parseTildes, require_parser2.parseXRanges, require_parser2.parseStar)(range);
    }
    function parseRange(range) {
      return require_utils5.pipe(require_parser2.parseHyphen, require_parser2.parseComparatorTrim, require_parser2.parseTildeTrim, require_parser2.parseCaretTrim)(range.trim()).split(/\s+/).join(" ");
    }
    function satisfy(version, range) {
      if (!version) return false;
      const extractedVersion = require_utils5.extractComparator(version);
      if (!extractedVersion) return false;
      const [, versionOperator, , versionMajor, versionMinor, versionPatch, versionPreRelease] = extractedVersion;
      const versionAtom = {
        operator: versionOperator,
        version: require_utils5.combineVersion(versionMajor, versionMinor, versionPatch, versionPreRelease),
        major: versionMajor,
        minor: versionMinor,
        patch: versionPatch,
        preRelease: versionPreRelease == null ? void 0 : versionPreRelease.split(".")
      };
      const orRanges = range.split("||");
      for (const orRange of orRanges) {
        const trimmedOrRange = orRange.trim();
        if (!trimmedOrRange) return true;
        if (trimmedOrRange === "*" || trimmedOrRange === "x") return true;
        try {
          const parsedSubRange = parseRange(trimmedOrRange);
          if (!parsedSubRange.trim()) return true;
          const parsedComparatorString = parsedSubRange.split(" ").map((rangeVersion) => parseComparatorString(rangeVersion)).join(" ");
          if (!parsedComparatorString.trim()) return true;
          const comparators = parsedComparatorString.split(/\s+/).map((comparator) => require_parser2.parseGTE0(comparator)).filter(Boolean);
          if (comparators.length === 0) continue;
          let subRangeSatisfied = true;
          for (const comparator of comparators) {
            const extractedComparator = require_utils5.extractComparator(comparator);
            if (!extractedComparator) {
              subRangeSatisfied = false;
              break;
            }
            const [, rangeOperator, , rangeMajor, rangeMinor, rangePatch, rangePreRelease] = extractedComparator;
            if (!require_compare2.compare({
              operator: rangeOperator,
              version: require_utils5.combineVersion(rangeMajor, rangeMinor, rangePatch, rangePreRelease),
              major: rangeMajor,
              minor: rangeMinor,
              patch: rangePatch,
              preRelease: rangePreRelease == null ? void 0 : rangePreRelease.split(".")
            }, versionAtom)) {
              subRangeSatisfied = false;
              break;
            }
          }
          if (subRangeSatisfied) return true;
        } catch (e) {
          console.error(`[semver] Error processing range part "${trimmedOrRange}":`, e);
          continue;
        }
      }
      return false;
    }
    exports2.satisfy = satisfy;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/constant.cjs
var require_constant2 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/constant.cjs"(exports2) {
    var DEFAULT_SCOPE = "default";
    var DEFAULT_REMOTE_TYPE = "global";
    exports2.DEFAULT_REMOTE_TYPE = DEFAULT_REMOTE_TYPE;
    exports2.DEFAULT_SCOPE = DEFAULT_SCOPE;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/share.cjs
var require_share = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/share.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_global2 = require_global();
    var require_constant3 = require_constant2();
    var require_index = require_semver();
    var _module_federation_sdk = require_dist();
    function formatShare(shareArgs, from, name, shareStrategy) {
      var _a, _b;
      let get;
      if ("get" in shareArgs) get = shareArgs.get;
      else if ("lib" in shareArgs) get = () => Promise.resolve(shareArgs.lib);
      else get = () => Promise.resolve(() => {
        require_logger3.error(`Cannot get shared "${name}" from "${from}": neither "get" nor "lib" is provided in the share config.`);
      });
      if (((_a = shareArgs.shareConfig) == null ? void 0 : _a.eager) && ((_b = shareArgs.treeShaking) == null ? void 0 : _b.mode)) require_logger3.error(`Invalid shared config for "${name}" from "${from}": cannot use both "eager: true" and "treeShaking.mode" simultaneously. Choose one strategy.`);
      return {
        deps: [],
        useIn: [],
        from,
        loading: null,
        ...shareArgs,
        shareConfig: {
          requiredVersion: `^${shareArgs.version}`,
          singleton: false,
          eager: false,
          strictVersion: false,
          ...shareArgs.shareConfig
        },
        get,
        loaded: (shareArgs == null ? void 0 : shareArgs.loaded) || "lib" in shareArgs ? true : void 0,
        version: shareArgs.version ?? "0",
        scope: Array.isArray(shareArgs.scope) ? shareArgs.scope : [shareArgs.scope ?? "default"],
        strategy: (shareArgs.strategy ?? shareStrategy) || "version-first",
        treeShaking: shareArgs.treeShaking ? {
          ...shareArgs.treeShaking,
          mode: shareArgs.treeShaking.mode ?? "server-calc",
          status: shareArgs.treeShaking.status ?? _module_federation_sdk.TreeShakingStatus.UNKNOWN,
          useIn: []
        } : void 0
      };
    }
    function formatShareConfigs(prevOptions, newOptions) {
      const shareArgs = newOptions.shared || {};
      const from = newOptions.name;
      const newShareInfos = Object.keys(shareArgs).reduce((res2, pkgName) => {
        const arrayShareArgs = require_tool2.arrayOptions(shareArgs[pkgName]);
        res2[pkgName] = res2[pkgName] || [];
        arrayShareArgs.forEach((shareConfig) => {
          res2[pkgName].push(formatShare(shareConfig, from, pkgName, newOptions.shareStrategy));
        });
        return res2;
      }, {});
      const allShareInfos = { ...prevOptions.shared };
      Object.keys(newShareInfos).forEach((shareKey) => {
        if (!allShareInfos[shareKey]) allShareInfos[shareKey] = newShareInfos[shareKey];
        else newShareInfos[shareKey].forEach((newUserSharedOptions) => {
          if (!allShareInfos[shareKey].find((sharedVal) => sharedVal.version === newUserSharedOptions.version)) allShareInfos[shareKey].push(newUserSharedOptions);
        });
      });
      return {
        allShareInfos,
        newShareInfos
      };
    }
    function shouldUseTreeShaking(treeShaking, usedExports) {
      if (!treeShaking) return false;
      const { status, mode } = treeShaking;
      if (status === _module_federation_sdk.TreeShakingStatus.NO_USE) return false;
      if (status === _module_federation_sdk.TreeShakingStatus.CALCULATED) return true;
      if (mode === "runtime-infer") {
        if (!usedExports) return true;
        return isMatchUsedExports(treeShaking, usedExports);
      }
      return false;
    }
    function versionLt(a, b) {
      const transformInvalidVersion = (version) => {
        if (!Number.isNaN(Number(version))) {
          const splitArr = version.split(".");
          let validVersion = version;
          for (let i = 0; i < 3 - splitArr.length; i++) validVersion += ".0";
          return validVersion;
        }
        return version;
      };
      if (require_index.satisfy(transformInvalidVersion(a), `<=${transformInvalidVersion(b)}`)) return true;
      else return false;
    }
    var findVersion = (shareVersionMap, cb2) => {
      const callback = cb2 || function(prev, cur) {
        return versionLt(prev, cur);
      };
      return Object.keys(shareVersionMap).reduce((prev, cur) => {
        if (!prev) return cur;
        if (callback(prev, cur)) return cur;
        if (prev === "0") return cur;
        return prev;
      }, 0);
    };
    var isLoaded = (shared) => {
      return Boolean(shared.loaded) || typeof shared.lib === "function";
    };
    var isLoading = (shared) => {
      return Boolean(shared.loading);
    };
    var isMatchUsedExports = (treeShaking, usedExports) => {
      if (!treeShaking || !usedExports) return false;
      const { usedExports: treeShakingUsedExports } = treeShaking;
      if (!treeShakingUsedExports) return false;
      if (usedExports.every((e) => treeShakingUsedExports.includes(e))) return true;
      return false;
    };
    function findSingletonVersionOrderByVersion(shareScopeMap, scope, pkgName, treeShaking) {
      const versions = shareScopeMap[scope][pkgName];
      let version = "";
      let useTreesShaking = shouldUseTreeShaking(treeShaking);
      const callback = function(prev, cur) {
        if (useTreesShaking) {
          if (!versions[prev].treeShaking) return true;
          if (!versions[cur].treeShaking) return false;
          return !isLoaded(versions[prev].treeShaking) && versionLt(prev, cur);
        }
        return !isLoaded(versions[prev]) && versionLt(prev, cur);
      };
      if (useTreesShaking) {
        version = findVersion(shareScopeMap[scope][pkgName], callback);
        if (version) return {
          version,
          useTreesShaking
        };
        useTreesShaking = false;
      }
      return {
        version: findVersion(shareScopeMap[scope][pkgName], callback),
        useTreesShaking
      };
    }
    var isLoadingOrLoaded = (shared) => {
      return isLoaded(shared) || isLoading(shared);
    };
    function findSingletonVersionOrderByLoaded(shareScopeMap, scope, pkgName, treeShaking) {
      const versions = shareScopeMap[scope][pkgName];
      let version = "";
      let useTreesShaking = shouldUseTreeShaking(treeShaking);
      const callback = function(prev, cur) {
        if (useTreesShaking) {
          if (!versions[prev].treeShaking) return true;
          if (!versions[cur].treeShaking) return false;
          if (isLoadingOrLoaded(versions[cur].treeShaking)) if (isLoadingOrLoaded(versions[prev].treeShaking)) return Boolean(versionLt(prev, cur));
          else return true;
          if (isLoadingOrLoaded(versions[prev].treeShaking)) return false;
        }
        if (isLoadingOrLoaded(versions[cur])) if (isLoadingOrLoaded(versions[prev])) return Boolean(versionLt(prev, cur));
        else return true;
        if (isLoadingOrLoaded(versions[prev])) return false;
        return versionLt(prev, cur);
      };
      if (useTreesShaking) {
        version = findVersion(shareScopeMap[scope][pkgName], callback);
        if (version) return {
          version,
          useTreesShaking
        };
        useTreesShaking = false;
      }
      return {
        version: findVersion(shareScopeMap[scope][pkgName], callback),
        useTreesShaking
      };
    }
    function getFindShareFunction(strategy) {
      if (strategy === "loaded-first") return findSingletonVersionOrderByLoaded;
      return findSingletonVersionOrderByVersion;
    }
    function getRegisteredShare(localShareScopeMap, pkgName, shareInfo, resolveShare) {
      if (!localShareScopeMap) return;
      const { shareConfig, scope = require_constant3.DEFAULT_SCOPE, strategy, treeShaking } = shareInfo;
      const scopes = Array.isArray(scope) ? scope : [scope];
      for (const sc of scopes) if (shareConfig && localShareScopeMap[sc] && localShareScopeMap[sc][pkgName]) {
        const { requiredVersion } = shareConfig;
        const { version: maxOrSingletonVersion, useTreesShaking } = getFindShareFunction(strategy)(localShareScopeMap, sc, pkgName, treeShaking);
        const defaultResolver = () => {
          const shared = localShareScopeMap[sc][pkgName][maxOrSingletonVersion];
          if (shareConfig.singleton) {
            if (typeof requiredVersion === "string" && !require_index.satisfy(maxOrSingletonVersion, requiredVersion)) {
              const msg = `Version ${maxOrSingletonVersion} from ${maxOrSingletonVersion && shared.from} of shared singleton module ${pkgName} does not satisfy the requirement of ${shareInfo.from} which needs ${requiredVersion})`;
              if (shareConfig.strictVersion) require_logger3.error(msg);
              else require_logger3.warn(msg);
            }
            return {
              shared,
              useTreesShaking
            };
          } else {
            if (requiredVersion === false || requiredVersion === "*") return {
              shared,
              useTreesShaking
            };
            if (require_index.satisfy(maxOrSingletonVersion, requiredVersion)) return {
              shared,
              useTreesShaking
            };
            const _usedTreeShaking = shouldUseTreeShaking(treeShaking);
            if (_usedTreeShaking) for (const [versionKey, versionValue] of Object.entries(localShareScopeMap[sc][pkgName])) {
              if (!shouldUseTreeShaking(versionValue.treeShaking, treeShaking == null ? void 0 : treeShaking.usedExports)) continue;
              if (require_index.satisfy(versionKey, requiredVersion)) return {
                shared: versionValue,
                useTreesShaking: _usedTreeShaking
              };
            }
            for (const [versionKey, versionValue] of Object.entries(localShareScopeMap[sc][pkgName])) if (require_index.satisfy(versionKey, requiredVersion)) return {
              shared: versionValue,
              useTreesShaking: false
            };
          }
        };
        const params = {
          shareScopeMap: localShareScopeMap,
          scope: sc,
          pkgName,
          version: maxOrSingletonVersion,
          GlobalFederation: require_global2.Global.__FEDERATION__,
          shareInfo,
          resolver: defaultResolver
        };
        return (resolveShare.emit(params) || params).resolver();
      }
    }
    function getGlobalShareScope() {
      return require_global2.Global.__FEDERATION__.__SHARE__;
    }
    function getTargetSharedOptions(options) {
      const { pkgName, extraOptions, shareInfos } = options;
      const defaultResolver = (sharedOptions) => {
        if (!sharedOptions) return;
        const shareVersionMap = {};
        sharedOptions.forEach((shared) => {
          shareVersionMap[shared.version] = shared;
        });
        const callback = function(prev, cur) {
          return !isLoaded(shareVersionMap[prev]) && versionLt(prev, cur);
        };
        return shareVersionMap[findVersion(shareVersionMap, callback)];
      };
      const resolver = (extraOptions == null ? void 0 : extraOptions.resolver) ?? defaultResolver;
      const isPlainObject = (val) => {
        return val !== null && typeof val === "object" && !Array.isArray(val);
      };
      const merge = (...sources) => {
        const out = {};
        for (const src of sources) {
          if (!src) continue;
          for (const [key, value] of Object.entries(src)) {
            const prev = out[key];
            if (isPlainObject(prev) && isPlainObject(value)) out[key] = merge(prev, value);
            else if (value !== void 0) out[key] = value;
          }
        }
        return out;
      };
      return merge(resolver(shareInfos[pkgName]), extraOptions == null ? void 0 : extraOptions.customShareInfo);
    }
    var addUseIn = (shared, from) => {
      if (!shared.useIn) shared.useIn = [];
      require_tool2.addUniqueItem(shared.useIn, from);
    };
    function directShare(shared, useTreesShaking) {
      if (useTreesShaking && shared.treeShaking) return shared.treeShaking;
      return shared;
    }
    exports2.addUseIn = addUseIn;
    exports2.directShare = directShare;
    exports2.formatShareConfigs = formatShareConfigs;
    exports2.getGlobalShareScope = getGlobalShareScope;
    exports2.getRegisteredShare = getRegisteredShare;
    exports2.getTargetSharedOptions = getTargetSharedOptions;
    exports2.shouldUseTreeShaking = shouldUseTreeShaking;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/manifest.cjs
var require_manifest = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/manifest.cjs"(exports2) {
    function matchRemoteWithNameAndExpose(remotes, id) {
      for (const remote of remotes) {
        const isNameMatched = id.startsWith(remote.name);
        let expose = id.replace(remote.name, "");
        if (isNameMatched) {
          if (expose.startsWith("/")) {
            const pkgNameOrAlias = remote.name;
            expose = `.${expose}`;
            return {
              pkgNameOrAlias,
              expose,
              remote
            };
          } else if (expose === "") return {
            pkgNameOrAlias: remote.name,
            expose: ".",
            remote
          };
        }
        const isAliasMatched = remote.alias && id.startsWith(remote.alias);
        let exposeWithAlias = remote.alias && id.replace(remote.alias, "");
        if (remote.alias && isAliasMatched) {
          if (exposeWithAlias && exposeWithAlias.startsWith("/")) {
            const pkgNameOrAlias = remote.alias;
            exposeWithAlias = `.${exposeWithAlias}`;
            return {
              pkgNameOrAlias,
              expose: exposeWithAlias,
              remote
            };
          } else if (exposeWithAlias === "") return {
            pkgNameOrAlias: remote.alias,
            expose: ".",
            remote
          };
        }
      }
    }
    function matchRemote(remotes, nameOrAlias) {
      for (const remote of remotes) {
        if (nameOrAlias === remote.name) return remote;
        if (remote.alias && nameOrAlias === remote.alias) return remote;
      }
    }
    exports2.matchRemote = matchRemote;
    exports2.matchRemoteWithNameAndExpose = matchRemoteWithNameAndExpose;
  }
});

// node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/error-codes.cjs
var require_error_codes = __commonJS({
  "node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/error-codes.cjs"(exports2) {
    var RUNTIME_001 = "RUNTIME-001";
    var RUNTIME_002 = "RUNTIME-002";
    var RUNTIME_003 = "RUNTIME-003";
    var RUNTIME_004 = "RUNTIME-004";
    var RUNTIME_005 = "RUNTIME-005";
    var RUNTIME_006 = "RUNTIME-006";
    var RUNTIME_007 = "RUNTIME-007";
    var RUNTIME_008 = "RUNTIME-008";
    var RUNTIME_009 = "RUNTIME-009";
    var RUNTIME_010 = "RUNTIME-010";
    var RUNTIME_011 = "RUNTIME-011";
    var TYPE_001 = "TYPE-001";
    var BUILD_001 = "BUILD-001";
    var BUILD_002 = "BUILD-002";
    exports2.BUILD_001 = BUILD_001;
    exports2.BUILD_002 = BUILD_002;
    exports2.RUNTIME_001 = RUNTIME_001;
    exports2.RUNTIME_002 = RUNTIME_002;
    exports2.RUNTIME_003 = RUNTIME_003;
    exports2.RUNTIME_004 = RUNTIME_004;
    exports2.RUNTIME_005 = RUNTIME_005;
    exports2.RUNTIME_006 = RUNTIME_006;
    exports2.RUNTIME_007 = RUNTIME_007;
    exports2.RUNTIME_008 = RUNTIME_008;
    exports2.RUNTIME_009 = RUNTIME_009;
    exports2.RUNTIME_010 = RUNTIME_010;
    exports2.RUNTIME_011 = RUNTIME_011;
    exports2.TYPE_001 = TYPE_001;
  }
});

// node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/desc.cjs
var require_desc = __commonJS({
  "node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/desc.cjs"(exports2) {
    var require_error_codes2 = require_error_codes();
    var runtimeDescMap = {
      [require_error_codes2.RUNTIME_001]: "Failed to get remoteEntry exports.",
      [require_error_codes2.RUNTIME_002]: 'The remote entry interface does not contain "init"',
      [require_error_codes2.RUNTIME_003]: "Failed to get manifest.",
      [require_error_codes2.RUNTIME_004]: "Failed to locate remote.",
      [require_error_codes2.RUNTIME_005]: "Invalid loadShareSync function call from bundler runtime",
      [require_error_codes2.RUNTIME_006]: "Invalid loadShareSync function call from runtime",
      [require_error_codes2.RUNTIME_007]: "Failed to get remote snapshot.",
      [require_error_codes2.RUNTIME_008]: "Failed to load script resources.",
      [require_error_codes2.RUNTIME_009]: "Please call createInstance first.",
      [require_error_codes2.RUNTIME_010]: 'The name option cannot be changed after initialization. If you want to create a new instance with a different name, please use "createInstance" api.',
      [require_error_codes2.RUNTIME_011]: "The remoteEntry URL is missing from the remote snapshot."
    };
    var typeDescMap = { [require_error_codes2.TYPE_001]: "Failed to generate type declaration. Execute the below cmd to reproduce and fix the error." };
    var buildDescMap = {
      [require_error_codes2.BUILD_001]: "Failed to find expose module.",
      [require_error_codes2.BUILD_002]: "PublicPath is required in prod mode."
    };
    var errorDescMap = {
      ...runtimeDescMap,
      ...typeDescMap,
      ...buildDescMap
    };
    exports2.buildDescMap = buildDescMap;
    exports2.errorDescMap = errorDescMap;
    exports2.runtimeDescMap = runtimeDescMap;
    exports2.typeDescMap = typeDescMap;
  }
});

// node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/index.cjs
var require_dist2 = __commonJS({
  "node_modules/.pnpm/@module-federation+error-codes@2.2.2/node_modules/@module-federation/error-codes/dist/index.cjs"(exports2) {
    Object.defineProperty(exports2, Symbol.toStringTag, { value: "Module" });
    var require_error_codes2 = require_error_codes();
    var require_getShortErrorMsg2 = require_getShortErrorMsg();
    var require_desc2 = require_desc();
    exports2.BUILD_001 = require_error_codes2.BUILD_001;
    exports2.BUILD_002 = require_error_codes2.BUILD_002;
    exports2.RUNTIME_001 = require_error_codes2.RUNTIME_001;
    exports2.RUNTIME_002 = require_error_codes2.RUNTIME_002;
    exports2.RUNTIME_003 = require_error_codes2.RUNTIME_003;
    exports2.RUNTIME_004 = require_error_codes2.RUNTIME_004;
    exports2.RUNTIME_005 = require_error_codes2.RUNTIME_005;
    exports2.RUNTIME_006 = require_error_codes2.RUNTIME_006;
    exports2.RUNTIME_007 = require_error_codes2.RUNTIME_007;
    exports2.RUNTIME_008 = require_error_codes2.RUNTIME_008;
    exports2.RUNTIME_009 = require_error_codes2.RUNTIME_009;
    exports2.RUNTIME_010 = require_error_codes2.RUNTIME_010;
    exports2.RUNTIME_011 = require_error_codes2.RUNTIME_011;
    exports2.TYPE_001 = require_error_codes2.TYPE_001;
    exports2.buildDescMap = require_desc2.buildDescMap;
    exports2.errorDescMap = require_desc2.errorDescMap;
    exports2.getShortErrorMsg = require_getShortErrorMsg2.getShortErrorMsg;
    exports2.runtimeDescMap = require_desc2.runtimeDescMap;
    exports2.typeDescMap = require_desc2.typeDescMap;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/load.cjs
var require_load = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/load.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_global2 = require_global();
    var require_constant3 = require_constant2();
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes = require_dist2();
    var importCallback = ".then(callbacks[0]).catch(callbacks[1])";
    async function loadEsmEntry({ entry, remoteEntryExports }) {
      return new Promise((resolve, reject) => {
        try {
          if (!remoteEntryExports) if (typeof FEDERATION_ALLOW_NEW_FUNCTION !== "undefined") new Function("callbacks", `import("${entry}")${importCallback}`)([resolve, reject]);
          else import(
            /* webpackIgnore: true */
            /* @vite-ignore */
            entry
          ).then(resolve).catch(reject);
          else resolve(remoteEntryExports);
        } catch (e) {
          require_logger3.error(`Failed to load ESM entry from "${entry}". ${e instanceof Error ? e.message : String(e)}`);
        }
      });
    }
    async function loadSystemJsEntry({ entry, remoteEntryExports }) {
      return new Promise((resolve, reject) => {
        try {
          if (!remoteEntryExports) if (typeof __system_context__ === "undefined") System.import(entry).then(resolve).catch(reject);
          else new Function("callbacks", `System.import("${entry}")${importCallback}`)([resolve, reject]);
          else resolve(remoteEntryExports);
        } catch (e) {
          require_logger3.error(`Failed to load SystemJS entry from "${entry}". ${e instanceof Error ? e.message : String(e)}`);
        }
      });
    }
    function handleRemoteEntryLoaded(name, globalName, entry) {
      const { remoteEntryKey, entryExports } = require_global2.getRemoteEntryExports(name, globalName);
      if (!entryExports) require_logger3.error(_module_federation_error_codes.RUNTIME_001, _module_federation_error_codes.runtimeDescMap, {
        remoteName: name,
        remoteEntryUrl: entry,
        remoteEntryKey
      });
      return entryExports;
    }
    async function loadEntryScript({ name, globalName, entry, loaderHook: loaderHook2, getEntryUrl }) {
      const { entryExports: remoteEntryExports } = require_global2.getRemoteEntryExports(name, globalName);
      if (remoteEntryExports) return remoteEntryExports;
      const url2 = getEntryUrl ? getEntryUrl(entry) : entry;
      return (0, _module_federation_sdk.loadScript)(url2, {
        attrs: {},
        createScriptHook: (url3, attrs2) => {
          const res2 = loaderHook2.lifecycle.createScript.emit({
            url: url3,
            attrs: attrs2
          });
          if (!res2) return;
          if (res2 instanceof HTMLScriptElement) return res2;
          if ("script" in res2 || "timeout" in res2) return res2;
        }
      }).then(() => {
        return handleRemoteEntryLoaded(name, globalName, entry);
      }, (loadError) => {
        const originalMsg = loadError instanceof Error ? loadError.message : String(loadError);
        require_logger3.error(_module_federation_error_codes.RUNTIME_008, _module_federation_error_codes.runtimeDescMap, {
          remoteName: name,
          resourceUrl: url2
        }, originalMsg);
      });
    }
    async function loadEntryDom({ remoteInfo, remoteEntryExports, loaderHook: loaderHook2, getEntryUrl }) {
      const { entry, entryGlobalName: globalName, name, type } = remoteInfo;
      switch (type) {
        case "esm":
        case "module":
          return loadEsmEntry({
            entry,
            remoteEntryExports
          });
        case "system":
          return loadSystemJsEntry({
            entry,
            remoteEntryExports
          });
        default:
          return loadEntryScript({
            entry,
            globalName,
            name,
            loaderHook: loaderHook2,
            getEntryUrl
          });
      }
    }
    async function loadEntryNode({ remoteInfo, loaderHook: loaderHook2 }) {
      const { entry, entryGlobalName: globalName, name, type } = remoteInfo;
      const { entryExports: remoteEntryExports } = require_global2.getRemoteEntryExports(name, globalName);
      if (remoteEntryExports) return remoteEntryExports;
      return (0, _module_federation_sdk.loadScriptNode)(entry, {
        attrs: {
          name,
          globalName,
          type
        },
        loaderHook: { createScriptHook: (url2, attrs2 = {}) => {
          const res2 = loaderHook2.lifecycle.createScript.emit({
            url: url2,
            attrs: attrs2
          });
          if (!res2) return;
          if ("url" in res2) return res2;
        } }
      }).then(() => {
        return handleRemoteEntryLoaded(name, globalName, entry);
      }).catch((e) => {
        require_logger3.error(`Failed to load Node.js entry for remote "${name}" from "${entry}". ${e instanceof Error ? e.message : String(e)}`);
      });
    }
    function getRemoteEntryUniqueKey(remoteInfo) {
      const { entry, name } = remoteInfo;
      return (0, _module_federation_sdk.composeKeyWithSeparator)(name, entry);
    }
    async function getRemoteEntry(params) {
      const { origin, remoteEntryExports, remoteInfo, getEntryUrl, _inErrorHandling = false } = params;
      const uniqueKey = getRemoteEntryUniqueKey(remoteInfo);
      if (remoteEntryExports) return remoteEntryExports;
      if (!require_global2.globalLoading[uniqueKey]) {
        const loadEntryHook = origin.remoteHandler.hooks.lifecycle.loadEntry;
        const loaderHook2 = origin.loaderHook;
        require_global2.globalLoading[uniqueKey] = loadEntryHook.emit({
          loaderHook: loaderHook2,
          remoteInfo,
          remoteEntryExports
        }).then((res2) => {
          if (res2) return res2;
          return (typeof ENV_TARGET !== "undefined" ? ENV_TARGET === "web" : _module_federation_sdk.isBrowserEnvValue) ? loadEntryDom({
            remoteInfo,
            remoteEntryExports,
            loaderHook: loaderHook2,
            getEntryUrl
          }) : loadEntryNode({
            remoteInfo,
            loaderHook: loaderHook2
          });
        }).catch(async (err) => {
          const uniqueKey2 = getRemoteEntryUniqueKey(remoteInfo);
          const isScriptExecutionError = err instanceof Error && err.message.includes("ScriptExecutionError");
          if (err instanceof Error && err.message.includes(_module_federation_error_codes.RUNTIME_008) && !isScriptExecutionError && !_inErrorHandling) {
            const wrappedGetRemoteEntry = (params2) => {
              return getRemoteEntry({
                ...params2,
                _inErrorHandling: true
              });
            };
            const RemoteEntryExports = await origin.loaderHook.lifecycle.loadEntryError.emit({
              getRemoteEntry: wrappedGetRemoteEntry,
              origin,
              remoteInfo,
              remoteEntryExports,
              globalLoading: require_global2.globalLoading,
              uniqueKey: uniqueKey2
            });
            if (RemoteEntryExports) return RemoteEntryExports;
          }
          throw err;
        });
      }
      return require_global2.globalLoading[uniqueKey];
    }
    function getRemoteInfo(remote) {
      return {
        ...remote,
        entry: "entry" in remote ? remote.entry : "",
        type: remote.type || require_constant3.DEFAULT_REMOTE_TYPE,
        entryGlobalName: remote.entryGlobalName || remote.name,
        shareScope: remote.shareScope || require_constant3.DEFAULT_SCOPE
      };
    }
    exports2.getRemoteEntry = getRemoteEntry;
    exports2.getRemoteEntryUniqueKey = getRemoteEntryUniqueKey;
    exports2.getRemoteInfo = getRemoteInfo;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/env.cjs
var require_env2 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/env.cjs"(exports2) {
    var _module_federation_sdk = require_dist();
    function getBuilderId() {
      return typeof FEDERATION_BUILD_IDENTIFIER !== "undefined" ? FEDERATION_BUILD_IDENTIFIER : "";
    }
    exports2.getBuilderId = getBuilderId;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/plugin.cjs
var require_plugin = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/plugin.cjs"(exports2) {
    var require_global2 = require_global();
    function registerPlugins(plugins, instance) {
      const globalPlugins = require_global2.getGlobalHostPlugins();
      const hookInstances = [
        instance.hooks,
        instance.remoteHandler.hooks,
        instance.sharedHandler.hooks,
        instance.snapshotHandler.hooks,
        instance.loaderHook,
        instance.bridgeHook
      ];
      if (globalPlugins.length > 0) globalPlugins.forEach((plugin) => {
        if (plugins == null ? void 0 : plugins.find((item) => item.name !== plugin.name)) plugins.push(plugin);
      });
      if (plugins && plugins.length > 0) plugins.forEach((plugin) => {
        hookInstances.forEach((hookInstance) => {
          hookInstance.applyPlugin(plugin, instance);
        });
      });
      return plugins;
    }
    exports2.registerPlugins = registerPlugins;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/context.cjs
var require_context = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/context.cjs"(exports2) {
    function remoteToEntry(r) {
      return {
        name: r.name,
        alias: r.alias,
        entry: "entry" in r ? r.entry : void 0,
        version: "version" in r ? r.version : void 0,
        type: r.type,
        entryGlobalName: r.entryGlobalName,
        shareScope: r.shareScope
      };
    }
    function optionsToMFContext(options) {
      var _a, _b, _c, _d, _e, _f;
      const shared = {};
      for (const [pkgName, versions] of Object.entries(options.shared)) {
        const first = versions[0];
        if (first) shared[pkgName] = {
          version: first.version,
          singleton: (_a = first.shareConfig) == null ? void 0 : _a.singleton,
          requiredVersion: ((_b = first.shareConfig) == null ? void 0 : _b.requiredVersion) === false ? false : (_c = first.shareConfig) == null ? void 0 : _c.requiredVersion,
          eager: first.eager,
          strictVersion: (_d = first.shareConfig) == null ? void 0 : _d.strictVersion
        };
      }
      return {
        project: {
          name: options.name,
          mfRole: ((_e = options.remotes) == null ? void 0 : _e.length) > 0 ? "host" : "unknown"
        },
        mfConfig: {
          name: options.name,
          remotes: ((_f = options.remotes) == null ? void 0 : _f.map(remoteToEntry)) ?? [],
          shared
        }
      };
    }
    exports2.optionsToMFContext = optionsToMFContext;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/index.cjs
var require_utils3 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/index.cjs"() {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_env3 = require_env2();
    var require_manifest2 = require_manifest();
    var require_plugin2 = require_plugin();
    var require_load2 = require_load();
    var require_context2 = require_context();
    var _module_federation_sdk = require_dist();
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/preload.cjs
var require_preload = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/preload.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_manifest2 = require_manifest();
    var require_load2 = require_load();
    var _module_federation_sdk = require_dist();
    function defaultPreloadArgs(preloadConfig) {
      return {
        resourceCategory: "sync",
        share: true,
        depsRemote: true,
        prefetchInterface: false,
        ...preloadConfig
      };
    }
    function formatPreloadArgs(remotes, preloadArgs) {
      return preloadArgs.map((args) => {
        const remoteInfo = require_manifest2.matchRemote(remotes, args.nameOrAlias);
        require_logger3.assert(remoteInfo, `Unable to preload ${args.nameOrAlias} as it is not included in ${!remoteInfo && (0, _module_federation_sdk.safeToString)({
          remoteInfo,
          remotes
        })}`);
        return {
          remote: remoteInfo,
          preloadConfig: defaultPreloadArgs(args)
        };
      });
    }
    function normalizePreloadExposes(exposes) {
      if (!exposes) return [];
      return exposes.map((expose) => {
        if (expose === ".") return expose;
        if (expose.startsWith("./")) return expose.replace("./", "");
        return expose;
      });
    }
    function preloadAssets(remoteInfo, host, assets, useLinkPreload = true) {
      const { cssAssets, jsAssetsWithoutEntry, entryAssets } = assets;
      if (host.options.inBrowser) {
        entryAssets.forEach((asset) => {
          const { moduleInfo } = asset;
          const module3 = host.moduleCache.get(remoteInfo.name);
          if (module3) require_load2.getRemoteEntry({
            origin: host,
            remoteInfo: moduleInfo,
            remoteEntryExports: module3.remoteEntryExports
          });
          else require_load2.getRemoteEntry({
            origin: host,
            remoteInfo: moduleInfo,
            remoteEntryExports: void 0
          });
        });
        if (useLinkPreload) {
          const defaultAttrs = {
            rel: "preload",
            as: "style"
          };
          cssAssets.forEach((cssUrl) => {
            const { link: cssEl, needAttach } = (0, _module_federation_sdk.createLink)({
              url: cssUrl,
              cb: () => {
              },
              attrs: defaultAttrs,
              createLinkHook: (url2, attrs2) => {
                const res2 = host.loaderHook.lifecycle.createLink.emit({
                  url: url2,
                  attrs: attrs2
                });
                if (res2 instanceof HTMLLinkElement) return res2;
              }
            });
            needAttach && document.head.appendChild(cssEl);
          });
        } else {
          const defaultAttrs = {
            rel: "stylesheet",
            type: "text/css"
          };
          cssAssets.forEach((cssUrl) => {
            const { link: cssEl, needAttach } = (0, _module_federation_sdk.createLink)({
              url: cssUrl,
              cb: () => {
              },
              attrs: defaultAttrs,
              createLinkHook: (url2, attrs2) => {
                const res2 = host.loaderHook.lifecycle.createLink.emit({
                  url: url2,
                  attrs: attrs2
                });
                if (res2 instanceof HTMLLinkElement) return res2;
              },
              needDeleteLink: false
            });
            needAttach && document.head.appendChild(cssEl);
          });
        }
        if (useLinkPreload) {
          const defaultAttrs = {
            rel: "preload",
            as: "script"
          };
          jsAssetsWithoutEntry.forEach((jsUrl) => {
            const { link: linkEl, needAttach } = (0, _module_federation_sdk.createLink)({
              url: jsUrl,
              cb: () => {
              },
              attrs: defaultAttrs,
              createLinkHook: (url2, attrs2) => {
                const res2 = host.loaderHook.lifecycle.createLink.emit({
                  url: url2,
                  attrs: attrs2
                });
                if (res2 instanceof HTMLLinkElement) return res2;
              }
            });
            needAttach && document.head.appendChild(linkEl);
          });
        } else {
          const defaultAttrs = {
            fetchpriority: "high",
            type: (remoteInfo == null ? void 0 : remoteInfo.type) === "module" ? "module" : "text/javascript"
          };
          jsAssetsWithoutEntry.forEach((jsUrl) => {
            const { script: scriptEl, needAttach } = (0, _module_federation_sdk.createScript)({
              url: jsUrl,
              cb: () => {
              },
              attrs: defaultAttrs,
              createScriptHook: (url2, attrs2) => {
                const res2 = host.loaderHook.lifecycle.createScript.emit({
                  url: url2,
                  attrs: attrs2
                });
                if (res2 instanceof HTMLScriptElement) return res2;
              },
              needDeleteScript: true
            });
            needAttach && document.head.appendChild(scriptEl);
          });
        }
      }
    }
    exports2.defaultPreloadArgs = defaultPreloadArgs;
    exports2.formatPreloadArgs = formatPreloadArgs;
    exports2.normalizePreloadExposes = normalizePreloadExposes;
    exports2.preloadAssets = preloadAssets;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/helpers.cjs
var require_helpers = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/helpers.cjs"(exports2) {
    var require_global2 = require_global();
    var require_share2 = require_share();
    var require_manifest2 = require_manifest();
    var require_load2 = require_load();
    require_utils3();
    var require_preload2 = require_preload();
    var ShareUtils = {
      getRegisteredShare: require_share2.getRegisteredShare,
      getGlobalShareScope: require_share2.getGlobalShareScope
    };
    var GlobalUtils = {
      Global: require_global2.Global,
      nativeGlobal: require_global2.nativeGlobal,
      resetFederationGlobalInfo: require_global2.resetFederationGlobalInfo,
      setGlobalFederationInstance: require_global2.setGlobalFederationInstance,
      getGlobalFederationConstructor: require_global2.getGlobalFederationConstructor,
      setGlobalFederationConstructor: require_global2.setGlobalFederationConstructor,
      getInfoWithoutType: require_global2.getInfoWithoutType,
      getGlobalSnapshot: require_global2.getGlobalSnapshot,
      getTargetSnapshotInfoByModuleInfo: require_global2.getTargetSnapshotInfoByModuleInfo,
      getGlobalSnapshotInfoByModuleInfo: require_global2.getGlobalSnapshotInfoByModuleInfo,
      setGlobalSnapshotInfoByModuleInfo: require_global2.setGlobalSnapshotInfoByModuleInfo,
      addGlobalSnapshot: require_global2.addGlobalSnapshot,
      getRemoteEntryExports: require_global2.getRemoteEntryExports,
      registerGlobalPlugins: require_global2.registerGlobalPlugins,
      getGlobalHostPlugins: require_global2.getGlobalHostPlugins,
      getPreloaded: require_global2.getPreloaded,
      setPreloaded: require_global2.setPreloaded
    };
    var helpers_default = {
      global: GlobalUtils,
      share: ShareUtils,
      utils: {
        matchRemoteWithNameAndExpose: require_manifest2.matchRemoteWithNameAndExpose,
        preloadAssets: require_preload2.preloadAssets,
        getRemoteInfo: require_load2.getRemoteInfo
      }
    };
    exports2.default = helpers_default;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/module/index.cjs
var require_module = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/module/index.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_load2 = require_load();
    var require_context2 = require_context();
    require_utils3();
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes = require_dist2();
    function createRemoteEntryInitOptions(remoteInfo, hostShareScopeMap, rawInitScope) {
      const localShareScopeMap = hostShareScopeMap;
      const shareScopeKeys = Array.isArray(remoteInfo.shareScope) ? remoteInfo.shareScope : [remoteInfo.shareScope];
      if (!shareScopeKeys.length) shareScopeKeys.push("default");
      shareScopeKeys.forEach((shareScopeKey) => {
        if (!localShareScopeMap[shareScopeKey]) localShareScopeMap[shareScopeKey] = {};
      });
      const remoteEntryInitOptions = {
        version: remoteInfo.version || "",
        shareScopeKeys: Array.isArray(remoteInfo.shareScope) ? shareScopeKeys : remoteInfo.shareScope || "default"
      };
      Object.defineProperty(remoteEntryInitOptions, "shareScopeMap", {
        value: localShareScopeMap,
        enumerable: false
      });
      return {
        remoteEntryInitOptions,
        shareScope: localShareScopeMap[shareScopeKeys[0]],
        initScope: rawInitScope ?? []
      };
    }
    var Module = class {
      constructor({ remoteInfo, host }) {
        this.inited = false;
        this.initing = false;
        this.lib = void 0;
        this.remoteInfo = remoteInfo;
        this.host = host;
      }
      async getEntry() {
        if (this.remoteEntryExports) return this.remoteEntryExports;
        const remoteEntryExports = await require_load2.getRemoteEntry({
          origin: this.host,
          remoteInfo: this.remoteInfo,
          remoteEntryExports: this.remoteEntryExports
        });
        require_logger3.assert(remoteEntryExports, `remoteEntryExports is undefined 
 ${(0, _module_federation_sdk.safeToString)(this.remoteInfo)}`);
        this.remoteEntryExports = remoteEntryExports;
        return this.remoteEntryExports;
      }
      async init(id, remoteSnapshot, rawInitScope) {
        const remoteEntryExports = await this.getEntry();
        if (this.inited) return remoteEntryExports;
        if (this.initPromise) {
          await this.initPromise;
          return remoteEntryExports;
        }
        this.initing = true;
        this.initPromise = (async () => {
          const { remoteEntryInitOptions, shareScope, initScope } = createRemoteEntryInitOptions(this.remoteInfo, this.host.shareScopeMap, rawInitScope);
          const initContainerOptions = await this.host.hooks.lifecycle.beforeInitContainer.emit({
            shareScope,
            remoteEntryInitOptions,
            initScope,
            remoteInfo: this.remoteInfo,
            origin: this.host
          });
          if (typeof (remoteEntryExports == null ? void 0 : remoteEntryExports.init) === "undefined") require_logger3.error(_module_federation_error_codes.RUNTIME_002, _module_federation_error_codes.runtimeDescMap, {
            hostName: this.host.name,
            remoteName: this.remoteInfo.name,
            remoteEntryUrl: this.remoteInfo.entry,
            remoteEntryKey: this.remoteInfo.entryGlobalName
          }, void 0, require_context2.optionsToMFContext(this.host.options));
          await remoteEntryExports.init(initContainerOptions.shareScope, initContainerOptions.initScope, initContainerOptions.remoteEntryInitOptions);
          await this.host.hooks.lifecycle.initContainer.emit({
            ...initContainerOptions,
            id,
            remoteSnapshot,
            remoteEntryExports
          });
          this.inited = true;
        })();
        try {
          await this.initPromise;
        } finally {
          this.initing = false;
          this.initPromise = void 0;
        }
        return remoteEntryExports;
      }
      async get(id, expose, options, remoteSnapshot) {
        const { loadFactory = true } = options || { loadFactory: true };
        const remoteEntryExports = await this.init(id, remoteSnapshot);
        this.lib = remoteEntryExports;
        let moduleFactory;
        moduleFactory = await this.host.loaderHook.lifecycle.getModuleFactory.emit({
          remoteEntryExports,
          expose,
          moduleInfo: this.remoteInfo
        });
        if (!moduleFactory) moduleFactory = await remoteEntryExports.get(expose);
        require_logger3.assert(moduleFactory, `${require_tool2.getFMId(this.remoteInfo)} remote don't export ${expose}.`);
        const symbolName = require_tool2.processModuleAlias(this.remoteInfo.name, expose);
        const wrapModuleFactory = this.wraperFactory(moduleFactory, symbolName);
        if (!loadFactory) return wrapModuleFactory;
        return await wrapModuleFactory();
      }
      wraperFactory(moduleFactory, id) {
        function defineModuleId(res2, id2) {
          if (res2 && typeof res2 === "object" && Object.isExtensible(res2) && !Object.getOwnPropertyDescriptor(res2, Symbol.for("mf_module_id"))) Object.defineProperty(res2, Symbol.for("mf_module_id"), {
            value: id2,
            enumerable: false
          });
        }
        if (moduleFactory instanceof Promise) return async () => {
          const res2 = await moduleFactory();
          defineModuleId(res2, id);
          return res2;
        };
        else return () => {
          const res2 = moduleFactory();
          defineModuleId(res2, id);
          return res2;
        };
      }
    };
    exports2.Module = Module;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/syncHook.cjs
var require_syncHook = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/syncHook.cjs"(exports2) {
    var SyncHook = class {
      constructor(type) {
        this.type = "";
        this.listeners = /* @__PURE__ */ new Set();
        if (type) this.type = type;
      }
      on(fn) {
        if (typeof fn === "function") this.listeners.add(fn);
      }
      once(fn) {
        const self = this;
        this.on(function wrapper(...args) {
          self.remove(wrapper);
          return fn.apply(null, args);
        });
      }
      emit(...data2) {
        let result;
        if (this.listeners.size > 0) this.listeners.forEach((fn) => {
          result = fn(...data2);
        });
        return result;
      }
      remove(fn) {
        this.listeners.delete(fn);
      }
      removeAll() {
        this.listeners.clear();
      }
    };
    exports2.SyncHook = SyncHook;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/asyncHook.cjs
var require_asyncHook = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/asyncHook.cjs"(exports2) {
    var require_syncHook2 = require_syncHook();
    var AsyncHook = class extends require_syncHook2.SyncHook {
      emit(...data2) {
        let result;
        const ls = Array.from(this.listeners);
        if (ls.length > 0) {
          let i = 0;
          const call = (prev) => {
            if (prev === false) return false;
            else if (i < ls.length) return Promise.resolve(ls[i++].apply(null, data2)).then(call);
            else return prev;
          };
          result = call();
        }
        return Promise.resolve(result);
      }
    };
    exports2.AsyncHook = AsyncHook;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/syncWaterfallHook.cjs
var require_syncWaterfallHook = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/syncWaterfallHook.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_syncHook2 = require_syncHook();
    function checkReturnData(originalData, returnedData) {
      if (!require_tool2.isObject(returnedData)) return false;
      if (originalData !== returnedData) {
        for (const key in originalData) if (!(key in returnedData)) return false;
      }
      return true;
    }
    var SyncWaterfallHook = class extends require_syncHook2.SyncHook {
      constructor(type) {
        super();
        this.onerror = require_logger3.error;
        this.type = type;
      }
      emit(data2) {
        if (!require_tool2.isObject(data2)) require_logger3.error(`The data for the "${this.type}" hook should be an object.`);
        for (const fn of this.listeners) try {
          const tempData = fn(data2);
          if (checkReturnData(data2, tempData)) data2 = tempData;
          else {
            this.onerror(`A plugin returned an unacceptable value for the "${this.type}" type.`);
            break;
          }
        } catch (e) {
          require_logger3.warn(e);
          this.onerror(e);
        }
        return data2;
      }
    };
    exports2.SyncWaterfallHook = SyncWaterfallHook;
    exports2.checkReturnData = checkReturnData;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/asyncWaterfallHooks.cjs
var require_asyncWaterfallHooks = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/asyncWaterfallHooks.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_syncHook2 = require_syncHook();
    var require_syncWaterfallHook2 = require_syncWaterfallHook();
    var AsyncWaterfallHook = class extends require_syncHook2.SyncHook {
      constructor(type) {
        super();
        this.onerror = require_logger3.error;
        this.type = type;
      }
      emit(data2) {
        if (!require_tool2.isObject(data2)) require_logger3.error(`The response data for the "${this.type}" hook must be an object.`);
        const ls = Array.from(this.listeners);
        if (ls.length > 0) {
          let i = 0;
          const processError = (e) => {
            require_logger3.warn(e);
            this.onerror(e);
            return data2;
          };
          const call = (prevData) => {
            if (require_syncWaterfallHook2.checkReturnData(data2, prevData)) {
              data2 = prevData;
              if (i < ls.length) try {
                return Promise.resolve(ls[i++](data2)).then(call, processError);
              } catch (e) {
                return processError(e);
              }
            } else this.onerror(`A plugin returned an incorrect value for the "${this.type}" type.`);
            return data2;
          };
          return Promise.resolve(call(data2));
        }
        return Promise.resolve(data2);
      }
    };
    exports2.AsyncWaterfallHook = AsyncWaterfallHook;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/pluginSystem.cjs
var require_pluginSystem = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/pluginSystem.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    require_utils3();
    var PluginSystem = class {
      constructor(lifecycle) {
        this.registerPlugins = {};
        this.lifecycle = lifecycle;
        this.lifecycleKeys = Object.keys(lifecycle);
      }
      applyPlugin(plugin, instance) {
        var _a;
        require_logger3.assert(require_tool2.isPlainObject(plugin), "Plugin configuration is invalid.");
        const pluginName = plugin.name;
        require_logger3.assert(pluginName, "A name must be provided by the plugin.");
        if (!this.registerPlugins[pluginName]) {
          this.registerPlugins[pluginName] = plugin;
          (_a = plugin.apply) == null ? void 0 : _a.call(plugin, instance);
          Object.keys(this.lifecycle).forEach((key) => {
            const pluginLife = plugin[key];
            if (pluginLife) this.lifecycle[key].on(pluginLife);
          });
        }
      }
      removePlugin(pluginName) {
        require_logger3.assert(pluginName, "A name is required.");
        const plugin = this.registerPlugins[pluginName];
        require_logger3.assert(plugin, `The plugin "${pluginName}" is not registered.`);
        Object.keys(plugin).forEach((key) => {
          if (key !== "name") this.lifecycle[key].remove(plugin[key]);
        });
      }
    };
    exports2.PluginSystem = PluginSystem;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/index.cjs
var require_hooks = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/utils/hooks/index.cjs"() {
    var require_syncHook2 = require_syncHook();
    var require_asyncHook2 = require_asyncHook();
    var require_syncWaterfallHook2 = require_syncWaterfallHook();
    var require_asyncWaterfallHooks2 = require_asyncWaterfallHooks();
    var require_pluginSystem2 = require_pluginSystem();
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/plugins/snapshot/index.cjs
var require_snapshot = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/plugins/snapshot/index.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    require_utils3();
    var require_preload2 = require_preload();
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes = require_dist2();
    function assignRemoteInfo(remoteInfo, remoteSnapshot) {
      const remoteEntryInfo = require_tool2.getRemoteEntryInfoFromSnapshot(remoteSnapshot);
      if (!remoteEntryInfo.url) require_logger3.error(_module_federation_error_codes.RUNTIME_011, _module_federation_error_codes.runtimeDescMap, { remoteName: remoteInfo.name });
      let entryUrl = (0, _module_federation_sdk.getResourceUrl)(remoteSnapshot, remoteEntryInfo.url);
      if (!_module_federation_sdk.isBrowserEnvValue && !entryUrl.startsWith("http")) entryUrl = `https:${entryUrl}`;
      remoteInfo.type = remoteEntryInfo.type;
      remoteInfo.entryGlobalName = remoteEntryInfo.globalName;
      remoteInfo.entry = entryUrl;
      remoteInfo.version = remoteSnapshot.version;
      remoteInfo.buildVersion = remoteSnapshot.buildVersion;
    }
    function snapshotPlugin() {
      return {
        name: "snapshot-plugin",
        async afterResolve(args) {
          const { remote, pkgNameOrAlias, expose, origin, remoteInfo, id } = args;
          if (!require_tool2.isRemoteInfoWithEntry(remote) || !require_tool2.isPureRemoteEntry(remote)) {
            const { remoteSnapshot, globalSnapshot } = await origin.snapshotHandler.loadRemoteSnapshotInfo({
              moduleInfo: remote,
              id
            });
            assignRemoteInfo(remoteInfo, remoteSnapshot);
            const preloadOptions = {
              remote,
              preloadConfig: {
                nameOrAlias: pkgNameOrAlias,
                exposes: [expose],
                resourceCategory: "sync",
                share: false,
                depsRemote: false
              }
            };
            const assets = await origin.remoteHandler.hooks.lifecycle.generatePreloadAssets.emit({
              origin,
              preloadOptions,
              remoteInfo,
              remote,
              remoteSnapshot,
              globalSnapshot
            });
            if (assets) require_preload2.preloadAssets(remoteInfo, origin, assets, false);
            return {
              ...args,
              remoteSnapshot
            };
          }
          return args;
        }
      };
    }
    exports2.assignRemoteInfo = assignRemoteInfo;
    exports2.snapshotPlugin = snapshotPlugin;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/plugins/generate-preload-assets.cjs
var require_generate_preload_assets = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/plugins/generate-preload-assets.cjs"(exports2) {
    var require_tool2 = require_tool();
    var require_global2 = require_global();
    var require_share2 = require_share();
    require_utils3();
    var require_preload2 = require_preload();
    var require_index$1 = require_snapshot();
    var _module_federation_sdk = require_dist();
    function splitId(id) {
      const splitInfo = id.split(":");
      if (splitInfo.length === 1) return {
        name: splitInfo[0],
        version: void 0
      };
      else if (splitInfo.length === 2) return {
        name: splitInfo[0],
        version: splitInfo[1]
      };
      else return {
        name: splitInfo[1],
        version: splitInfo[2]
      };
    }
    function traverseModuleInfo(globalSnapshot, remoteInfo, traverse, isRoot, memo = {}, remoteSnapshot) {
      const { value: snapshotValue } = require_global2.getInfoWithoutType(globalSnapshot, require_tool2.getFMId(remoteInfo));
      const effectiveRemoteSnapshot = remoteSnapshot || snapshotValue;
      if (effectiveRemoteSnapshot && !(0, _module_federation_sdk.isManifestProvider)(effectiveRemoteSnapshot)) {
        traverse(effectiveRemoteSnapshot, remoteInfo, isRoot);
        if (effectiveRemoteSnapshot.remotesInfo) {
          const remoteKeys = Object.keys(effectiveRemoteSnapshot.remotesInfo);
          for (const key of remoteKeys) {
            if (memo[key]) continue;
            memo[key] = true;
            const subRemoteInfo = splitId(key);
            const remoteValue = effectiveRemoteSnapshot.remotesInfo[key];
            traverseModuleInfo(globalSnapshot, {
              name: subRemoteInfo.name,
              version: remoteValue.matchedVersion
            }, traverse, false, memo, void 0);
          }
        }
      }
    }
    var isExisted = (type, url2) => {
      return document.querySelector(`${type}[${type === "link" ? "href" : "src"}="${url2}"]`);
    };
    function generatePreloadAssets(origin, preloadOptions, remote, globalSnapshot, remoteSnapshot) {
      const cssAssets = [];
      const jsAssets = [];
      const entryAssets = [];
      const loadedSharedJsAssets = /* @__PURE__ */ new Set();
      const loadedSharedCssAssets = /* @__PURE__ */ new Set();
      const { options } = origin;
      const { preloadConfig: rootPreloadConfig } = preloadOptions;
      const { depsRemote } = rootPreloadConfig;
      traverseModuleInfo(globalSnapshot, remote, (moduleInfoSnapshot, remoteInfo, isRoot) => {
        var _a;
        let preloadConfig;
        if (isRoot) preloadConfig = rootPreloadConfig;
        else if (Array.isArray(depsRemote)) {
          const findPreloadConfig = depsRemote.find((remoteConfig) => {
            if (remoteConfig.nameOrAlias === remoteInfo.name || remoteConfig.nameOrAlias === remoteInfo.alias) return true;
            return false;
          });
          if (!findPreloadConfig) return;
          preloadConfig = require_preload2.defaultPreloadArgs(findPreloadConfig);
        } else if (depsRemote === true) preloadConfig = rootPreloadConfig;
        else return;
        const remoteEntryUrl = (0, _module_federation_sdk.getResourceUrl)(moduleInfoSnapshot, require_tool2.getRemoteEntryInfoFromSnapshot(moduleInfoSnapshot).url);
        if (remoteEntryUrl) entryAssets.push({
          name: remoteInfo.name,
          moduleInfo: {
            name: remoteInfo.name,
            entry: remoteEntryUrl,
            type: "remoteEntryType" in moduleInfoSnapshot ? moduleInfoSnapshot.remoteEntryType : "global",
            entryGlobalName: "globalName" in moduleInfoSnapshot ? moduleInfoSnapshot.globalName : remoteInfo.name,
            shareScope: "",
            version: "version" in moduleInfoSnapshot ? moduleInfoSnapshot.version : void 0
          },
          url: remoteEntryUrl
        });
        let moduleAssetsInfo = "modules" in moduleInfoSnapshot ? moduleInfoSnapshot.modules : [];
        const normalizedPreloadExposes = require_preload2.normalizePreloadExposes(preloadConfig.exposes);
        if (normalizedPreloadExposes.length && "modules" in moduleInfoSnapshot) moduleAssetsInfo = (_a = moduleInfoSnapshot == null ? void 0 : moduleInfoSnapshot.modules) == null ? void 0 : _a.reduce((assets, moduleAssetInfo) => {
          if ((normalizedPreloadExposes == null ? void 0 : normalizedPreloadExposes.indexOf(moduleAssetInfo.moduleName)) !== -1) assets.push(moduleAssetInfo);
          return assets;
        }, []);
        function handleAssets(assets) {
          const assetsRes = assets.map((asset) => (0, _module_federation_sdk.getResourceUrl)(moduleInfoSnapshot, asset));
          if (preloadConfig.filter) return assetsRes.filter(preloadConfig.filter);
          return assetsRes;
        }
        if (moduleAssetsInfo) {
          const assetsLength = moduleAssetsInfo.length;
          for (let index = 0; index < assetsLength; index++) {
            const assetsInfo = moduleAssetsInfo[index];
            const exposeFullPath = `${remoteInfo.name}/${assetsInfo.moduleName}`;
            origin.remoteHandler.hooks.lifecycle.handlePreloadModule.emit({
              id: assetsInfo.moduleName === "." ? remoteInfo.name : exposeFullPath,
              name: remoteInfo.name,
              remoteSnapshot: moduleInfoSnapshot,
              preloadConfig,
              remote: remoteInfo,
              origin
            });
            if (require_global2.getPreloaded(exposeFullPath)) continue;
            if (preloadConfig.resourceCategory === "all") {
              cssAssets.push(...handleAssets(assetsInfo.assets.css.async));
              cssAssets.push(...handleAssets(assetsInfo.assets.css.sync));
              jsAssets.push(...handleAssets(assetsInfo.assets.js.async));
              jsAssets.push(...handleAssets(assetsInfo.assets.js.sync));
            } else if (preloadConfig.resourceCategory === "sync") {
              cssAssets.push(...handleAssets(assetsInfo.assets.css.sync));
              jsAssets.push(...handleAssets(assetsInfo.assets.js.sync));
            }
            require_global2.setPreloaded(exposeFullPath);
          }
        }
      }, true, {}, remoteSnapshot);
      if (remoteSnapshot.shared && remoteSnapshot.shared.length > 0) {
        const collectSharedAssets = (shareInfo, snapshotShared) => {
          const { shared: registeredShared } = require_share2.getRegisteredShare(origin.shareScopeMap, snapshotShared.sharedName, shareInfo, origin.sharedHandler.hooks.lifecycle.resolveShare) || {};
          if (registeredShared && typeof registeredShared.lib === "function") {
            snapshotShared.assets.js.sync.forEach((asset) => {
              loadedSharedJsAssets.add(asset);
            });
            snapshotShared.assets.css.sync.forEach((asset) => {
              loadedSharedCssAssets.add(asset);
            });
          }
        };
        remoteSnapshot.shared.forEach((shared) => {
          var _a;
          const shareInfos = (_a = options.shared) == null ? void 0 : _a[shared.sharedName];
          if (!shareInfos) return;
          const sharedOptions = shared.version ? shareInfos.find((s) => s.version === shared.version) : shareInfos;
          if (!sharedOptions) return;
          require_tool2.arrayOptions(sharedOptions).forEach((s) => {
            collectSharedAssets(s, shared);
          });
        });
      }
      const needPreloadJsAssets = jsAssets.filter((asset) => !loadedSharedJsAssets.has(asset) && !isExisted("script", asset));
      return {
        cssAssets: cssAssets.filter((asset) => !loadedSharedCssAssets.has(asset) && !isExisted("link", asset)),
        jsAssetsWithoutEntry: needPreloadJsAssets,
        entryAssets: entryAssets.filter((entry) => !isExisted("script", entry.url))
      };
    }
    var generatePreloadAssetsPlugin = function() {
      return {
        name: "generate-preload-assets-plugin",
        async generatePreloadAssets(args) {
          const { origin, preloadOptions, remoteInfo, remote, globalSnapshot, remoteSnapshot } = args;
          if (!_module_federation_sdk.isBrowserEnvValue) return {
            cssAssets: [],
            jsAssetsWithoutEntry: [],
            entryAssets: []
          };
          if (require_tool2.isRemoteInfoWithEntry(remote) && require_tool2.isPureRemoteEntry(remote)) return {
            cssAssets: [],
            jsAssetsWithoutEntry: [],
            entryAssets: [{
              name: remote.name,
              url: remote.entry,
              moduleInfo: {
                name: remoteInfo.name,
                entry: remote.entry,
                type: remoteInfo.type || "global",
                entryGlobalName: "",
                shareScope: ""
              }
            }]
          };
          require_index$1.assignRemoteInfo(remoteInfo, remoteSnapshot);
          return generatePreloadAssets(origin, preloadOptions, remoteInfo, globalSnapshot, remoteSnapshot);
        }
      };
    };
    exports2.generatePreloadAssetsPlugin = generatePreloadAssetsPlugin;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/plugins/snapshot/SnapshotHandler.cjs
var require_SnapshotHandler = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/plugins/snapshot/SnapshotHandler.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_global2 = require_global();
    var require_context2 = require_context();
    require_utils3();
    var require_asyncHook2 = require_asyncHook();
    var require_asyncWaterfallHooks2 = require_asyncWaterfallHooks();
    var require_pluginSystem2 = require_pluginSystem();
    require_hooks();
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes = require_dist2();
    function getGlobalRemoteInfo(moduleInfo, origin) {
      const hostGlobalSnapshot = require_global2.getGlobalSnapshotInfoByModuleInfo({
        name: origin.name,
        version: origin.options.version
      });
      const globalRemoteInfo = hostGlobalSnapshot && "remotesInfo" in hostGlobalSnapshot && hostGlobalSnapshot.remotesInfo && require_global2.getInfoWithoutType(hostGlobalSnapshot.remotesInfo, moduleInfo.name).value;
      if (globalRemoteInfo && globalRemoteInfo.matchedVersion) return {
        hostGlobalSnapshot,
        globalSnapshot: require_global2.getGlobalSnapshot(),
        remoteSnapshot: require_global2.getGlobalSnapshotInfoByModuleInfo({
          name: moduleInfo.name,
          version: globalRemoteInfo.matchedVersion
        })
      };
      return {
        hostGlobalSnapshot: void 0,
        globalSnapshot: require_global2.getGlobalSnapshot(),
        remoteSnapshot: require_global2.getGlobalSnapshotInfoByModuleInfo({
          name: moduleInfo.name,
          version: "version" in moduleInfo ? moduleInfo.version : void 0
        })
      };
    }
    var SnapshotHandler = class {
      constructor(HostInstance) {
        this.loadingHostSnapshot = null;
        this.manifestCache = /* @__PURE__ */ new Map();
        this.hooks = new require_pluginSystem2.PluginSystem({
          beforeLoadRemoteSnapshot: new require_asyncHook2.AsyncHook("beforeLoadRemoteSnapshot"),
          loadSnapshot: new require_asyncWaterfallHooks2.AsyncWaterfallHook("loadGlobalSnapshot"),
          loadRemoteSnapshot: new require_asyncWaterfallHooks2.AsyncWaterfallHook("loadRemoteSnapshot"),
          afterLoadSnapshot: new require_asyncWaterfallHooks2.AsyncWaterfallHook("afterLoadSnapshot")
        });
        this.manifestLoading = require_global2.Global.__FEDERATION__.__MANIFEST_LOADING__;
        this.HostInstance = HostInstance;
        this.loaderHook = HostInstance.loaderHook;
      }
      async loadRemoteSnapshotInfo({ moduleInfo, id, expose }) {
        const { options } = this.HostInstance;
        await this.hooks.lifecycle.beforeLoadRemoteSnapshot.emit({
          options,
          moduleInfo
        });
        let hostSnapshot = require_global2.getGlobalSnapshotInfoByModuleInfo({
          name: this.HostInstance.options.name,
          version: this.HostInstance.options.version
        });
        if (!hostSnapshot) {
          hostSnapshot = {
            version: this.HostInstance.options.version || "",
            remoteEntry: "",
            remotesInfo: {}
          };
          require_global2.addGlobalSnapshot({ [this.HostInstance.options.name]: hostSnapshot });
        }
        if (hostSnapshot && "remotesInfo" in hostSnapshot && !require_global2.getInfoWithoutType(hostSnapshot.remotesInfo, moduleInfo.name).value) {
          if ("version" in moduleInfo || "entry" in moduleInfo) hostSnapshot.remotesInfo = {
            ...hostSnapshot == null ? void 0 : hostSnapshot.remotesInfo,
            [moduleInfo.name]: { matchedVersion: "version" in moduleInfo ? moduleInfo.version : moduleInfo.entry }
          };
        }
        const { hostGlobalSnapshot, remoteSnapshot, globalSnapshot } = this.getGlobalRemoteInfo(moduleInfo);
        const { remoteSnapshot: globalRemoteSnapshot, globalSnapshot: globalSnapshotRes } = await this.hooks.lifecycle.loadSnapshot.emit({
          options,
          moduleInfo,
          hostGlobalSnapshot,
          remoteSnapshot,
          globalSnapshot
        });
        let mSnapshot;
        let gSnapshot;
        if (globalRemoteSnapshot) if ((0, _module_federation_sdk.isManifestProvider)(globalRemoteSnapshot)) {
          const remoteEntry = _module_federation_sdk.isBrowserEnvValue ? globalRemoteSnapshot.remoteEntry : globalRemoteSnapshot.ssrRemoteEntry || globalRemoteSnapshot.remoteEntry || "";
          const moduleSnapshot = await this.getManifestJson(remoteEntry, moduleInfo, {});
          const globalSnapshotRes2 = require_global2.setGlobalSnapshotInfoByModuleInfo({
            ...moduleInfo,
            entry: remoteEntry
          }, moduleSnapshot);
          mSnapshot = moduleSnapshot;
          gSnapshot = globalSnapshotRes2;
        } else {
          const { remoteSnapshot: remoteSnapshotRes } = await this.hooks.lifecycle.loadRemoteSnapshot.emit({
            options: this.HostInstance.options,
            moduleInfo,
            remoteSnapshot: globalRemoteSnapshot,
            from: "global"
          });
          mSnapshot = remoteSnapshotRes;
          gSnapshot = globalSnapshotRes;
        }
        else if (require_tool2.isRemoteInfoWithEntry(moduleInfo)) {
          const moduleSnapshot = await this.getManifestJson(moduleInfo.entry, moduleInfo, {});
          const globalSnapshotRes2 = require_global2.setGlobalSnapshotInfoByModuleInfo(moduleInfo, moduleSnapshot);
          const { remoteSnapshot: remoteSnapshotRes } = await this.hooks.lifecycle.loadRemoteSnapshot.emit({
            options: this.HostInstance.options,
            moduleInfo,
            remoteSnapshot: moduleSnapshot,
            from: "global"
          });
          mSnapshot = remoteSnapshotRes;
          gSnapshot = globalSnapshotRes2;
        } else require_logger3.error(_module_federation_error_codes.RUNTIME_007, _module_federation_error_codes.runtimeDescMap, {
          remoteName: moduleInfo.name,
          remoteVersion: moduleInfo.version,
          hostName: this.HostInstance.options.name,
          globalSnapshot: JSON.stringify(globalSnapshotRes)
        }, void 0, require_context2.optionsToMFContext(this.HostInstance.options));
        await this.hooks.lifecycle.afterLoadSnapshot.emit({
          id,
          host: this.HostInstance,
          options,
          moduleInfo,
          remoteSnapshot: mSnapshot
        });
        return {
          remoteSnapshot: mSnapshot,
          globalSnapshot: gSnapshot
        };
      }
      getGlobalRemoteInfo(moduleInfo) {
        return getGlobalRemoteInfo(moduleInfo, this.HostInstance);
      }
      async getManifestJson(manifestUrl, moduleInfo, extraOptions) {
        const getManifest = async () => {
          let manifestJson = this.manifestCache.get(manifestUrl);
          if (manifestJson) return manifestJson;
          try {
            let res2 = await this.loaderHook.lifecycle.fetch.emit(manifestUrl, {});
            if (!res2 || !(res2 instanceof Response)) res2 = await fetch(manifestUrl, {});
            manifestJson = await res2.json();
          } catch (err) {
            manifestJson = await this.HostInstance.remoteHandler.hooks.lifecycle.errorLoadRemote.emit({
              id: manifestUrl,
              error: err,
              from: "runtime",
              lifecycle: "afterResolve",
              origin: this.HostInstance
            });
            if (!manifestJson) {
              delete this.manifestLoading[manifestUrl];
              require_logger3.error(_module_federation_error_codes.RUNTIME_003, _module_federation_error_codes.runtimeDescMap, {
                manifestUrl,
                moduleName: moduleInfo.name,
                hostName: this.HostInstance.options.name
              }, `${err}`, require_context2.optionsToMFContext(this.HostInstance.options));
            }
          }
          require_logger3.assert(manifestJson.metaData && manifestJson.exposes && manifestJson.shared, `"${manifestUrl}" is not a valid federation manifest for remote "${moduleInfo.name}". Missing required fields: ${[
            !manifestJson.metaData && "metaData",
            !manifestJson.exposes && "exposes",
            !manifestJson.shared && "shared"
          ].filter(Boolean).join(", ")}.`);
          this.manifestCache.set(manifestUrl, manifestJson);
          return manifestJson;
        };
        const asyncLoadProcess = async () => {
          const manifestJson = await getManifest();
          const remoteSnapshot = (0, _module_federation_sdk.generateSnapshotFromManifest)(manifestJson, { version: manifestUrl });
          const { remoteSnapshot: remoteSnapshotRes } = await this.hooks.lifecycle.loadRemoteSnapshot.emit({
            options: this.HostInstance.options,
            moduleInfo,
            manifestJson,
            remoteSnapshot,
            manifestUrl,
            from: "manifest"
          });
          return remoteSnapshotRes;
        };
        if (!this.manifestLoading[manifestUrl]) this.manifestLoading[manifestUrl] = asyncLoadProcess().then((res2) => res2);
        return this.manifestLoading[manifestUrl];
      }
    };
    exports2.SnapshotHandler = SnapshotHandler;
    exports2.getGlobalRemoteInfo = getGlobalRemoteInfo;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/shared/index.cjs
var require_shared = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/shared/index.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_constant3 = require_constant2();
    var require_share2 = require_share();
    var require_context2 = require_context();
    require_utils3();
    var require_asyncHook2 = require_asyncHook();
    var require_syncWaterfallHook2 = require_syncWaterfallHook();
    var require_asyncWaterfallHooks2 = require_asyncWaterfallHooks();
    var require_pluginSystem2 = require_pluginSystem();
    require_hooks();
    var _module_federation_error_codes = require_dist2();
    var SharedHandler = class {
      constructor(host) {
        this.hooks = new require_pluginSystem2.PluginSystem({
          beforeRegisterShare: new require_syncWaterfallHook2.SyncWaterfallHook("beforeRegisterShare"),
          afterResolve: new require_asyncWaterfallHooks2.AsyncWaterfallHook("afterResolve"),
          beforeLoadShare: new require_asyncWaterfallHooks2.AsyncWaterfallHook("beforeLoadShare"),
          loadShare: new require_asyncHook2.AsyncHook(),
          resolveShare: new require_syncWaterfallHook2.SyncWaterfallHook("resolveShare"),
          initContainerShareScopeMap: new require_syncWaterfallHook2.SyncWaterfallHook("initContainerShareScopeMap")
        });
        this.host = host;
        this.shareScopeMap = {};
        this.initTokens = {};
        this._setGlobalShareScopeMap(host.options);
      }
      registerShared(globalOptions, userOptions) {
        const { newShareInfos, allShareInfos } = require_share2.formatShareConfigs(globalOptions, userOptions);
        Object.keys(newShareInfos).forEach((sharedKey) => {
          newShareInfos[sharedKey].forEach((sharedVal) => {
            sharedVal.scope.forEach((sc) => {
              var _a;
              this.hooks.lifecycle.beforeRegisterShare.emit({
                origin: this.host,
                pkgName: sharedKey,
                shared: sharedVal
              });
              if (!((_a = this.shareScopeMap[sc]) == null ? void 0 : _a[sharedKey])) this.setShared({
                pkgName: sharedKey,
                lib: sharedVal.lib,
                get: sharedVal.get,
                loaded: sharedVal.loaded || Boolean(sharedVal.lib),
                shared: sharedVal,
                from: userOptions.name
              });
            });
          });
        });
        return {
          newShareInfos,
          allShareInfos
        };
      }
      async loadShare(pkgName, extraOptions) {
        const { host } = this;
        const shareOptions = require_share2.getTargetSharedOptions({
          pkgName,
          extraOptions,
          shareInfos: host.options.shared
        });
        if (shareOptions == null ? void 0 : shareOptions.scope) await Promise.all(shareOptions.scope.map(async (shareScope) => {
          await Promise.all(this.initializeSharing(shareScope, { strategy: shareOptions.strategy }));
        }));
        const { shareInfo: shareOptionsRes } = await this.hooks.lifecycle.beforeLoadShare.emit({
          pkgName,
          shareInfo: shareOptions,
          shared: host.options.shared,
          origin: host
        });
        require_logger3.assert(shareOptionsRes, `Cannot find shared "${pkgName}" in host "${host.options.name}". Ensure the shared config for "${pkgName}" is declared in the federation plugin options and the host has been initialized before loading shares.`);
        const { shared: registeredShared, useTreesShaking } = require_share2.getRegisteredShare(this.shareScopeMap, pkgName, shareOptionsRes, this.hooks.lifecycle.resolveShare) || {};
        if (registeredShared) {
          const targetShared = require_share2.directShare(registeredShared, useTreesShaking);
          if (targetShared.lib) {
            require_share2.addUseIn(targetShared, host.options.name);
            return targetShared.lib;
          } else if (targetShared.loading && !targetShared.loaded) {
            const factory = await targetShared.loading;
            targetShared.loaded = true;
            if (!targetShared.lib) targetShared.lib = factory;
            require_share2.addUseIn(targetShared, host.options.name);
            return factory;
          } else {
            const asyncLoadProcess = async () => {
              const factory = await targetShared.get();
              require_share2.addUseIn(targetShared, host.options.name);
              targetShared.loaded = true;
              targetShared.lib = factory;
              return factory;
            };
            const loading = asyncLoadProcess();
            this.setShared({
              pkgName,
              loaded: false,
              shared: registeredShared,
              from: host.options.name,
              lib: null,
              loading,
              treeShaking: useTreesShaking ? targetShared : void 0
            });
            return loading;
          }
        } else {
          if (extraOptions == null ? void 0 : extraOptions.customShareInfo) return false;
          const _useTreeShaking = require_share2.shouldUseTreeShaking(shareOptionsRes.treeShaking);
          const targetShared = require_share2.directShare(shareOptionsRes, _useTreeShaking);
          const asyncLoadProcess = async () => {
            const factory = await targetShared.get();
            targetShared.lib = factory;
            targetShared.loaded = true;
            require_share2.addUseIn(targetShared, host.options.name);
            const { shared: gShared, useTreesShaking: gUseTreeShaking } = require_share2.getRegisteredShare(this.shareScopeMap, pkgName, shareOptionsRes, this.hooks.lifecycle.resolveShare) || {};
            if (gShared) {
              const targetGShared = require_share2.directShare(gShared, gUseTreeShaking);
              targetGShared.lib = factory;
              targetGShared.loaded = true;
              gShared.from = shareOptionsRes.from;
            }
            return factory;
          };
          const loading = asyncLoadProcess();
          this.setShared({
            pkgName,
            loaded: false,
            shared: shareOptionsRes,
            from: host.options.name,
            lib: null,
            loading,
            treeShaking: _useTreeShaking ? targetShared : void 0
          });
          return loading;
        }
      }
      /**
      * This function initializes the sharing sequence (executed only once per share scope).
      * It accepts one argument, the name of the share scope.
      * If the share scope does not exist, it creates one.
      */
      initializeSharing(shareScopeName = require_constant3.DEFAULT_SCOPE, extraOptions) {
        const { host } = this;
        const from = extraOptions == null ? void 0 : extraOptions.from;
        const strategy = extraOptions == null ? void 0 : extraOptions.strategy;
        let initScope = extraOptions == null ? void 0 : extraOptions.initScope;
        const promises = [];
        if (from !== "build") {
          const { initTokens } = this;
          if (!initScope) initScope = [];
          let initToken = initTokens[shareScopeName];
          if (!initToken) initToken = initTokens[shareScopeName] = { from: this.host.name };
          if (initScope.indexOf(initToken) >= 0) return promises;
          initScope.push(initToken);
        }
        const shareScope = this.shareScopeMap;
        const hostName = host.options.name;
        if (!shareScope[shareScopeName]) shareScope[shareScopeName] = {};
        const scope = shareScope[shareScopeName];
        const register = (name, shared) => {
          var _a;
          const { version, eager } = shared;
          scope[name] = scope[name] || {};
          const versions = scope[name];
          const activeVersion = versions[version] && require_share2.directShare(versions[version]);
          const activeVersionEager = Boolean(activeVersion && ("eager" in activeVersion && activeVersion.eager || "shareConfig" in activeVersion && ((_a = activeVersion.shareConfig) == null ? void 0 : _a.eager)));
          if (!activeVersion || activeVersion.strategy !== "loaded-first" && !activeVersion.loaded && (Boolean(!eager) !== !activeVersionEager ? eager : hostName > versions[version].from)) versions[version] = shared;
        };
        const initRemoteModule = async (key) => {
          const { module: module3 } = await host.remoteHandler.getRemoteModuleAndOptions({ id: key });
          let remoteEntryExports = void 0;
          try {
            remoteEntryExports = await module3.getEntry();
          } catch (error) {
            remoteEntryExports = await host.remoteHandler.hooks.lifecycle.errorLoadRemote.emit({
              id: key,
              error,
              from: "runtime",
              lifecycle: "beforeLoadShare",
              origin: host
            });
            if (!remoteEntryExports) return;
          } finally {
            if ((remoteEntryExports == null ? void 0 : remoteEntryExports.init) && !module3.initing) {
              module3.remoteEntryExports = remoteEntryExports;
              await module3.init(void 0, void 0, initScope);
            }
          }
        };
        Object.keys(host.options.shared).forEach((shareName) => {
          host.options.shared[shareName].forEach((shared) => {
            if (shared.scope.includes(shareScopeName)) register(shareName, shared);
          });
        });
        if (host.options.shareStrategy === "version-first" || strategy === "version-first") host.options.remotes.forEach((remote) => {
          if (remote.shareScope === shareScopeName) promises.push(initRemoteModule(remote.name));
        });
        return promises;
      }
      loadShareSync(pkgName, extraOptions) {
        const { host } = this;
        const shareOptions = require_share2.getTargetSharedOptions({
          pkgName,
          extraOptions,
          shareInfos: host.options.shared
        });
        if (shareOptions == null ? void 0 : shareOptions.scope) shareOptions.scope.forEach((shareScope) => {
          this.initializeSharing(shareScope, { strategy: shareOptions.strategy });
        });
        const { shared: registeredShared, useTreesShaking } = require_share2.getRegisteredShare(this.shareScopeMap, pkgName, shareOptions, this.hooks.lifecycle.resolveShare) || {};
        if (registeredShared) {
          if (typeof registeredShared.lib === "function") {
            require_share2.addUseIn(registeredShared, host.options.name);
            if (!registeredShared.loaded) {
              registeredShared.loaded = true;
              if (registeredShared.from === host.options.name) shareOptions.loaded = true;
            }
            return registeredShared.lib;
          }
          if (typeof registeredShared.get === "function") {
            const module3 = registeredShared.get();
            if (!(module3 instanceof Promise)) {
              require_share2.addUseIn(registeredShared, host.options.name);
              this.setShared({
                pkgName,
                loaded: true,
                from: host.options.name,
                lib: module3,
                shared: registeredShared
              });
              return module3;
            }
          }
        }
        if (shareOptions.lib) {
          if (!shareOptions.loaded) shareOptions.loaded = true;
          return shareOptions.lib;
        }
        if (shareOptions.get) {
          const module3 = shareOptions.get();
          if (module3 instanceof Promise) require_logger3.error((extraOptions == null ? void 0 : extraOptions.from) === "build" ? _module_federation_error_codes.RUNTIME_005 : _module_federation_error_codes.RUNTIME_006, _module_federation_error_codes.runtimeDescMap, {
            hostName: host.options.name,
            sharedPkgName: pkgName
          }, void 0, require_context2.optionsToMFContext(host.options));
          shareOptions.lib = module3;
          this.setShared({
            pkgName,
            loaded: true,
            from: host.options.name,
            lib: shareOptions.lib,
            shared: shareOptions
          });
          return shareOptions.lib;
        }
        require_logger3.error(_module_federation_error_codes.RUNTIME_006, _module_federation_error_codes.runtimeDescMap, {
          hostName: host.options.name,
          sharedPkgName: pkgName
        }, void 0, require_context2.optionsToMFContext(host.options));
      }
      initShareScopeMap(scopeName, shareScope, extraOptions = {}) {
        const { host } = this;
        this.shareScopeMap[scopeName] = shareScope;
        this.hooks.lifecycle.initContainerShareScopeMap.emit({
          shareScope,
          options: host.options,
          origin: host,
          scopeName,
          hostShareScopeMap: extraOptions.hostShareScopeMap
        });
      }
      setShared({ pkgName, shared, from, lib, loading, loaded, get, treeShaking }) {
        const { version, scope = "default", ...shareInfo } = shared;
        const scopes = Array.isArray(scope) ? scope : [scope];
        const mergeAttrs = (shared2) => {
          const merge = (s, key, val) => {
            if (val && !s[key]) s[key] = val;
          };
          const targetShared = treeShaking ? shared2.treeShaking : shared2;
          merge(targetShared, "loaded", loaded);
          merge(targetShared, "loading", loading);
          merge(targetShared, "get", get);
        };
        scopes.forEach((sc) => {
          if (!this.shareScopeMap[sc]) this.shareScopeMap[sc] = {};
          if (!this.shareScopeMap[sc][pkgName]) this.shareScopeMap[sc][pkgName] = {};
          if (!this.shareScopeMap[sc][pkgName][version]) this.shareScopeMap[sc][pkgName][version] = {
            version,
            scope: [sc],
            ...shareInfo,
            lib
          };
          const registeredShared = this.shareScopeMap[sc][pkgName][version];
          mergeAttrs(registeredShared);
          if (from && registeredShared.from !== from) registeredShared.from = from;
        });
      }
      _setGlobalShareScopeMap(hostOptions) {
        const globalShareScopeMap = require_share2.getGlobalShareScope();
        const identifier = hostOptions.id || hostOptions.name;
        if (identifier && !globalShareScopeMap[identifier]) globalShareScopeMap[identifier] = this.shareScopeMap;
      }
    };
    exports2.SharedHandler = SharedHandler;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/remote/index.cjs
var require_remote = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/remote/index.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_global2 = require_global();
    var require_constant3 = require_constant2();
    var require_share2 = require_share();
    var require_manifest2 = require_manifest();
    var require_load2 = require_load();
    var require_context2 = require_context();
    require_utils3();
    var require_preload2 = require_preload();
    var require_index$1 = require_module();
    var require_syncHook2 = require_syncHook();
    var require_asyncHook2 = require_asyncHook();
    var require_syncWaterfallHook2 = require_syncWaterfallHook();
    var require_asyncWaterfallHooks2 = require_asyncWaterfallHooks();
    var require_pluginSystem2 = require_pluginSystem();
    require_hooks();
    var require_SnapshotHandler2 = require_SnapshotHandler();
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes = require_dist2();
    var RemoteHandler = class {
      constructor(host) {
        this.hooks = new require_pluginSystem2.PluginSystem({
          beforeRegisterRemote: new require_syncWaterfallHook2.SyncWaterfallHook("beforeRegisterRemote"),
          registerRemote: new require_syncWaterfallHook2.SyncWaterfallHook("registerRemote"),
          beforeRequest: new require_asyncWaterfallHooks2.AsyncWaterfallHook("beforeRequest"),
          onLoad: new require_asyncHook2.AsyncHook("onLoad"),
          handlePreloadModule: new require_syncHook2.SyncHook("handlePreloadModule"),
          errorLoadRemote: new require_asyncHook2.AsyncHook("errorLoadRemote"),
          beforePreloadRemote: new require_asyncHook2.AsyncHook("beforePreloadRemote"),
          generatePreloadAssets: new require_asyncHook2.AsyncHook("generatePreloadAssets"),
          afterPreloadRemote: new require_asyncHook2.AsyncHook(),
          loadEntry: new require_asyncHook2.AsyncHook()
        });
        this.host = host;
        this.idToRemoteMap = {};
      }
      formatAndRegisterRemote(globalOptions, userOptions) {
        return (userOptions.remotes || []).reduce((res2, remote) => {
          this.registerRemote(remote, res2, { force: false });
          return res2;
        }, globalOptions.remotes);
      }
      setIdToRemoteMap(id, remoteMatchInfo) {
        const { remote, expose } = remoteMatchInfo;
        const { name, alias } = remote;
        this.idToRemoteMap[id] = {
          name: remote.name,
          expose
        };
        if (alias && id.startsWith(name)) {
          const idWithAlias = id.replace(name, alias);
          this.idToRemoteMap[idWithAlias] = {
            name: remote.name,
            expose
          };
          return;
        }
        if (alias && id.startsWith(alias)) {
          const idWithName = id.replace(alias, name);
          this.idToRemoteMap[idWithName] = {
            name: remote.name,
            expose
          };
        }
      }
      async loadRemote(id, options) {
        const { host } = this;
        try {
          const { loadFactory = true } = options || { loadFactory: true };
          const { module: module3, moduleOptions, remoteMatchInfo } = await this.getRemoteModuleAndOptions({ id });
          const { pkgNameOrAlias, remote, expose, id: idRes, remoteSnapshot } = remoteMatchInfo;
          const moduleOrFactory = await module3.get(idRes, expose, options, remoteSnapshot);
          const moduleWrapper = await this.hooks.lifecycle.onLoad.emit({
            id: idRes,
            pkgNameOrAlias,
            expose,
            exposeModule: loadFactory ? moduleOrFactory : void 0,
            exposeModuleFactory: loadFactory ? void 0 : moduleOrFactory,
            remote,
            options: moduleOptions,
            moduleInstance: module3,
            origin: host
          });
          this.setIdToRemoteMap(id, remoteMatchInfo);
          if (typeof moduleWrapper === "function") return moduleWrapper;
          return moduleOrFactory;
        } catch (error) {
          const { from = "runtime" } = options || { from: "runtime" };
          const failOver = await this.hooks.lifecycle.errorLoadRemote.emit({
            id,
            error,
            from,
            lifecycle: "onLoad",
            origin: host
          });
          if (!failOver) throw error;
          return failOver;
        }
      }
      async preloadRemote(preloadOptions) {
        const { host } = this;
        await this.hooks.lifecycle.beforePreloadRemote.emit({
          preloadOps: preloadOptions,
          options: host.options,
          origin: host
        });
        const preloadOps = require_preload2.formatPreloadArgs(host.options.remotes, preloadOptions);
        await Promise.all(preloadOps.map(async (ops) => {
          const { remote } = ops;
          const remoteInfo = require_load2.getRemoteInfo(remote);
          const { globalSnapshot, remoteSnapshot } = await host.snapshotHandler.loadRemoteSnapshotInfo({ moduleInfo: remote });
          const assets = await this.hooks.lifecycle.generatePreloadAssets.emit({
            origin: host,
            preloadOptions: ops,
            remote,
            remoteInfo,
            globalSnapshot,
            remoteSnapshot
          });
          if (!assets) return;
          require_preload2.preloadAssets(remoteInfo, host, assets);
        }));
      }
      registerRemotes(remotes, options) {
        const { host } = this;
        remotes.forEach((remote) => {
          this.registerRemote(remote, host.options.remotes, { force: options == null ? void 0 : options.force });
        });
      }
      async getRemoteModuleAndOptions(options) {
        const { host } = this;
        const { id } = options;
        let loadRemoteArgs;
        try {
          loadRemoteArgs = await this.hooks.lifecycle.beforeRequest.emit({
            id,
            options: host.options,
            origin: host
          });
        } catch (error) {
          loadRemoteArgs = await this.hooks.lifecycle.errorLoadRemote.emit({
            id,
            options: host.options,
            origin: host,
            from: "runtime",
            error,
            lifecycle: "beforeRequest"
          });
          if (!loadRemoteArgs) throw error;
        }
        const { id: idRes } = loadRemoteArgs;
        const remoteSplitInfo = require_manifest2.matchRemoteWithNameAndExpose(host.options.remotes, idRes);
        if (!remoteSplitInfo) require_logger3.error(_module_federation_error_codes.RUNTIME_004, _module_federation_error_codes.runtimeDescMap, {
          hostName: host.options.name,
          requestId: idRes
        }, void 0, require_context2.optionsToMFContext(host.options));
        const { remote: rawRemote } = remoteSplitInfo;
        const remoteInfo = require_load2.getRemoteInfo(rawRemote);
        const matchInfo = await host.sharedHandler.hooks.lifecycle.afterResolve.emit({
          id: idRes,
          ...remoteSplitInfo,
          options: host.options,
          origin: host,
          remoteInfo
        });
        const { remote, expose } = matchInfo;
        require_logger3.assert(remote && expose, `The 'beforeRequest' hook was executed, but it failed to return the correct 'remote' and 'expose' values while loading ${idRes}.`);
        let module3 = host.moduleCache.get(remote.name);
        const moduleOptions = {
          host,
          remoteInfo
        };
        if (!module3) {
          module3 = new require_index$1.Module(moduleOptions);
          host.moduleCache.set(remote.name, module3);
        }
        return {
          module: module3,
          moduleOptions,
          remoteMatchInfo: matchInfo
        };
      }
      registerRemote(remote, targetRemotes, options) {
        const { host } = this;
        const normalizeRemote = () => {
          if (remote.alias) {
            const findEqual = targetRemotes.find((item) => {
              var _a;
              return remote.alias && (item.name.startsWith(remote.alias) || ((_a = item.alias) == null ? void 0 : _a.startsWith(remote.alias)));
            });
            require_logger3.assert(!findEqual, `The alias ${remote.alias} of remote ${remote.name} is not allowed to be the prefix of ${findEqual && findEqual.name} name or alias`);
          }
          if ("entry" in remote) {
            if (_module_federation_sdk.isBrowserEnvValue && typeof window !== "undefined" && !remote.entry.startsWith("http")) remote.entry = new URL(remote.entry, window.location.origin).href;
          }
          if (!remote.shareScope) remote.shareScope = require_constant3.DEFAULT_SCOPE;
          if (!remote.type) remote.type = require_constant3.DEFAULT_REMOTE_TYPE;
        };
        this.hooks.lifecycle.beforeRegisterRemote.emit({
          remote,
          origin: host
        });
        const registeredRemote = targetRemotes.find((item) => item.name === remote.name);
        if (!registeredRemote) {
          normalizeRemote();
          targetRemotes.push(remote);
          this.hooks.lifecycle.registerRemote.emit({
            remote,
            origin: host
          });
        } else {
          const messages = [`The remote "${remote.name}" is already registered.`, "Please note that overriding it may cause unexpected errors."];
          if (options == null ? void 0 : options.force) {
            this.removeRemote(registeredRemote);
            normalizeRemote();
            targetRemotes.push(remote);
            this.hooks.lifecycle.registerRemote.emit({
              remote,
              origin: host
            });
            (0, _module_federation_sdk.warn)(messages.join(" "));
          }
        }
      }
      removeRemote(remote) {
        var _a;
        try {
          const { host } = this;
          const { name } = remote;
          const remoteIndex = host.options.remotes.findIndex((item) => item.name === name);
          if (remoteIndex !== -1) host.options.remotes.splice(remoteIndex, 1);
          const loadedModule = host.moduleCache.get(remote.name);
          if (loadedModule) {
            const remoteInfo = loadedModule.remoteInfo;
            const key = remoteInfo.entryGlobalName;
            if (require_global2.CurrentGlobal[key]) if ((_a = Object.getOwnPropertyDescriptor(require_global2.CurrentGlobal, key)) == null ? void 0 : _a.configurable) delete require_global2.CurrentGlobal[key];
            else require_global2.CurrentGlobal[key] = void 0;
            const remoteEntryUniqueKey = require_load2.getRemoteEntryUniqueKey(loadedModule.remoteInfo);
            if (require_global2.globalLoading[remoteEntryUniqueKey]) delete require_global2.globalLoading[remoteEntryUniqueKey];
            host.snapshotHandler.manifestCache.delete(remoteInfo.entry);
            let remoteInsId = remoteInfo.buildVersion ? (0, _module_federation_sdk.composeKeyWithSeparator)(remoteInfo.name, remoteInfo.buildVersion) : remoteInfo.name;
            const remoteInsIndex = require_global2.CurrentGlobal.__FEDERATION__.__INSTANCES__.findIndex((ins) => {
              if (remoteInfo.buildVersion) return ins.options.id === remoteInsId;
              else return ins.name === remoteInsId;
            });
            if (remoteInsIndex !== -1) {
              const remoteIns = require_global2.CurrentGlobal.__FEDERATION__.__INSTANCES__[remoteInsIndex];
              remoteInsId = remoteIns.options.id || remoteInsId;
              const globalShareScopeMap = require_share2.getGlobalShareScope();
              let isAllSharedNotUsed = true;
              const needDeleteKeys = [];
              Object.keys(globalShareScopeMap).forEach((instId) => {
                const shareScopeMap = globalShareScopeMap[instId];
                shareScopeMap && Object.keys(shareScopeMap).forEach((shareScope) => {
                  const shareScopeVal = shareScopeMap[shareScope];
                  shareScopeVal && Object.keys(shareScopeVal).forEach((shareName) => {
                    const sharedPkgs = shareScopeVal[shareName];
                    sharedPkgs && Object.keys(sharedPkgs).forEach((shareVersion) => {
                      const shared = sharedPkgs[shareVersion];
                      if (shared && typeof shared === "object" && shared.from === remoteInfo.name) if (shared.loaded || shared.loading) {
                        shared.useIn = shared.useIn.filter((usedHostName) => usedHostName !== remoteInfo.name);
                        if (shared.useIn.length) isAllSharedNotUsed = false;
                        else needDeleteKeys.push([
                          instId,
                          shareScope,
                          shareName,
                          shareVersion
                        ]);
                      } else needDeleteKeys.push([
                        instId,
                        shareScope,
                        shareName,
                        shareVersion
                      ]);
                    });
                  });
                });
              });
              if (isAllSharedNotUsed) {
                remoteIns.shareScopeMap = {};
                delete globalShareScopeMap[remoteInsId];
              }
              needDeleteKeys.forEach(([insId, shareScope, shareName, shareVersion]) => {
                var _a2, _b, _c;
                (_c = (_b = (_a2 = globalShareScopeMap[insId]) == null ? void 0 : _a2[shareScope]) == null ? void 0 : _b[shareName]) == null ? true : delete _c[shareVersion];
              });
              require_global2.CurrentGlobal.__FEDERATION__.__INSTANCES__.splice(remoteInsIndex, 1);
            }
            const { hostGlobalSnapshot } = require_SnapshotHandler2.getGlobalRemoteInfo(remote, host);
            if (hostGlobalSnapshot) {
              const remoteKey = hostGlobalSnapshot && "remotesInfo" in hostGlobalSnapshot && hostGlobalSnapshot.remotesInfo && require_global2.getInfoWithoutType(hostGlobalSnapshot.remotesInfo, remote.name).key;
              if (remoteKey) {
                delete hostGlobalSnapshot.remotesInfo[remoteKey];
                if (Boolean(require_global2.Global.__FEDERATION__.__MANIFEST_LOADING__[remoteKey])) delete require_global2.Global.__FEDERATION__.__MANIFEST_LOADING__[remoteKey];
              }
            }
            host.moduleCache.delete(remote.name);
          }
        } catch (err) {
          require_logger3.logger.error(`removeRemote failed: ${err instanceof Error ? err.message : String(err)}`);
        }
      }
    };
    exports2.RemoteHandler = RemoteHandler;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/core.cjs
var require_core = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/core.cjs"(exports2) {
    var require_logger3 = require_logger2();
    var require_constant3 = require_constant2();
    var require_share2 = require_share();
    var require_env3 = require_env2();
    var require_plugin2 = require_plugin();
    var require_load2 = require_load();
    require_utils3();
    var require_index$1 = require_module();
    var require_syncHook2 = require_syncHook();
    var require_asyncHook2 = require_asyncHook();
    var require_syncWaterfallHook2 = require_syncWaterfallHook();
    var require_asyncWaterfallHooks2 = require_asyncWaterfallHooks();
    var require_pluginSystem2 = require_pluginSystem();
    require_hooks();
    var require_index$3 = require_snapshot();
    var require_generate_preload_assets2 = require_generate_preload_assets();
    var require_SnapshotHandler2 = require_SnapshotHandler();
    var require_index$4 = require_shared();
    var require_index$5 = require_remote();
    var _module_federation_sdk = require_dist();
    var _module_federation_error_codes = require_dist2();
    var USE_SNAPSHOT = typeof FEDERATION_OPTIMIZE_NO_SNAPSHOT_PLUGIN === "boolean" ? !FEDERATION_OPTIMIZE_NO_SNAPSHOT_PLUGIN : true;
    var ModuleFederation = class {
      constructor(userOptions) {
        this.hooks = new require_pluginSystem2.PluginSystem({
          beforeInit: new require_syncWaterfallHook2.SyncWaterfallHook("beforeInit"),
          init: new require_syncHook2.SyncHook(),
          beforeInitContainer: new require_asyncWaterfallHooks2.AsyncWaterfallHook("beforeInitContainer"),
          initContainer: new require_asyncWaterfallHooks2.AsyncWaterfallHook("initContainer")
        });
        this.version = "2.2.2";
        this.moduleCache = /* @__PURE__ */ new Map();
        this.loaderHook = new require_pluginSystem2.PluginSystem({
          getModuleInfo: new require_syncHook2.SyncHook(),
          createScript: new require_syncHook2.SyncHook(),
          createLink: new require_syncHook2.SyncHook(),
          fetch: new require_asyncHook2.AsyncHook(),
          loadEntryError: new require_asyncHook2.AsyncHook(),
          getModuleFactory: new require_asyncHook2.AsyncHook()
        });
        this.bridgeHook = new require_pluginSystem2.PluginSystem({
          beforeBridgeRender: new require_syncHook2.SyncHook(),
          afterBridgeRender: new require_syncHook2.SyncHook(),
          beforeBridgeDestroy: new require_syncHook2.SyncHook(),
          afterBridgeDestroy: new require_syncHook2.SyncHook()
        });
        const plugins = USE_SNAPSHOT ? [require_index$3.snapshotPlugin(), require_generate_preload_assets2.generatePreloadAssetsPlugin()] : [];
        const defaultOptions = {
          id: require_env3.getBuilderId(),
          name: userOptions.name,
          plugins,
          remotes: [],
          shared: {},
          inBrowser: _module_federation_sdk.isBrowserEnvValue
        };
        this.name = userOptions.name;
        this.options = defaultOptions;
        this.snapshotHandler = new require_SnapshotHandler2.SnapshotHandler(this);
        this.sharedHandler = new require_index$4.SharedHandler(this);
        this.remoteHandler = new require_index$5.RemoteHandler(this);
        this.shareScopeMap = this.sharedHandler.shareScopeMap;
        this.registerPlugins([...defaultOptions.plugins, ...userOptions.plugins || []]);
        this.options = this.formatOptions(defaultOptions, userOptions);
      }
      initOptions(userOptions) {
        if (userOptions.name && userOptions.name !== this.options.name) require_logger3.error((0, _module_federation_error_codes.getShortErrorMsg)(_module_federation_error_codes.RUNTIME_010, _module_federation_error_codes.runtimeDescMap));
        this.registerPlugins(userOptions.plugins);
        const options = this.formatOptions(this.options, userOptions);
        this.options = options;
        return options;
      }
      async loadShare(pkgName, extraOptions) {
        return this.sharedHandler.loadShare(pkgName, extraOptions);
      }
      loadShareSync(pkgName, extraOptions) {
        return this.sharedHandler.loadShareSync(pkgName, extraOptions);
      }
      initializeSharing(shareScopeName = require_constant3.DEFAULT_SCOPE, extraOptions) {
        return this.sharedHandler.initializeSharing(shareScopeName, extraOptions);
      }
      initRawContainer(name, url2, container) {
        const remoteInfo = require_load2.getRemoteInfo({
          name,
          entry: url2
        });
        const module3 = new require_index$1.Module({
          host: this,
          remoteInfo
        });
        module3.remoteEntryExports = container;
        this.moduleCache.set(name, module3);
        return module3;
      }
      async loadRemote(id, options) {
        return this.remoteHandler.loadRemote(id, options);
      }
      async preloadRemote(preloadOptions) {
        return this.remoteHandler.preloadRemote(preloadOptions);
      }
      initShareScopeMap(scopeName, shareScope, extraOptions = {}) {
        this.sharedHandler.initShareScopeMap(scopeName, shareScope, extraOptions);
      }
      formatOptions(globalOptions, userOptions) {
        const { allShareInfos: shared } = require_share2.formatShareConfigs(globalOptions, userOptions);
        const { userOptions: userOptionsRes, options: globalOptionsRes } = this.hooks.lifecycle.beforeInit.emit({
          origin: this,
          userOptions,
          options: globalOptions,
          shareInfo: shared
        });
        const remotes = this.remoteHandler.formatAndRegisterRemote(globalOptionsRes, userOptionsRes);
        const { allShareInfos } = this.sharedHandler.registerShared(globalOptionsRes, userOptionsRes);
        const plugins = [...globalOptionsRes.plugins];
        if (userOptionsRes.plugins) userOptionsRes.plugins.forEach((plugin) => {
          if (!plugins.includes(plugin)) plugins.push(plugin);
        });
        const optionsRes = {
          ...globalOptions,
          ...userOptions,
          plugins,
          remotes,
          shared: allShareInfos
        };
        this.hooks.lifecycle.init.emit({
          origin: this,
          options: optionsRes
        });
        return optionsRes;
      }
      registerPlugins(plugins) {
        const pluginRes = require_plugin2.registerPlugins(plugins, this);
        this.options.plugins = this.options.plugins.reduce((res2, plugin) => {
          if (!plugin) return res2;
          if (res2 && !res2.find((item) => item.name === plugin.name)) res2.push(plugin);
          return res2;
        }, pluginRes || []);
      }
      registerRemotes(remotes, options) {
        return this.remoteHandler.registerRemotes(remotes, options);
      }
      registerShared(shared) {
        this.sharedHandler.registerShared(this.options, {
          ...this.options,
          shared
        });
      }
    };
    exports2.ModuleFederation = ModuleFederation;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/_virtual/_rolldown/runtime.cjs
var require_runtime3 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/_virtual/_rolldown/runtime.cjs"(exports2) {
    var __defProp = Object.defineProperty;
    var __exportAll = (all, no_symbols) => {
      let target = {};
      for (var name in all) {
        __defProp(target, name, {
          get: all[name],
          enumerable: true
        });
      }
      if (!no_symbols) {
        __defProp(target, Symbol.toStringTag, { value: "Module" });
      }
      return target;
    };
    exports2.__exportAll = __exportAll;
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/type/index.cjs
var require_type = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/type/index.cjs"(exports2) {
    var require_runtime4 = require_runtime3();
    var type_exports = require_runtime4.__exportAll({});
    Object.defineProperty(exports2, "type_exports", {
      enumerable: true,
      get: function() {
        return type_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/index.cjs
var require_dist3 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime-core@2.2.2/node_modules/@module-federation/runtime-core/dist/index.cjs"(exports2) {
    Object.defineProperty(exports2, Symbol.toStringTag, { value: "Module" });
    var require_logger3 = require_logger2();
    var require_tool2 = require_tool();
    var require_global2 = require_global();
    var require_index = require_semver();
    var require_share2 = require_share();
    var require_manifest2 = require_manifest();
    var require_load2 = require_load();
    require_utils3();
    var require_helpers2 = require_helpers();
    var require_index$2 = require_module();
    var require_core2 = require_core();
    var require_index$3 = require_type();
    var _module_federation_sdk = require_dist();
    var helpers = require_helpers2.default;
    exports2.CurrentGlobal = require_global2.CurrentGlobal;
    exports2.Global = require_global2.Global;
    exports2.Module = require_index$2.Module;
    exports2.ModuleFederation = require_core2.ModuleFederation;
    exports2.addGlobalSnapshot = require_global2.addGlobalSnapshot;
    exports2.assert = require_logger3.assert;
    exports2.error = require_logger3.error;
    exports2.getGlobalFederationConstructor = require_global2.getGlobalFederationConstructor;
    exports2.getGlobalSnapshot = require_global2.getGlobalSnapshot;
    exports2.getInfoWithoutType = require_global2.getInfoWithoutType;
    exports2.getRegisteredShare = require_share2.getRegisteredShare;
    exports2.getRemoteEntry = require_load2.getRemoteEntry;
    exports2.getRemoteInfo = require_load2.getRemoteInfo;
    exports2.helpers = helpers;
    exports2.isStaticResourcesEqual = require_tool2.isStaticResourcesEqual;
    Object.defineProperty(exports2, "loadScript", {
      enumerable: true,
      get: function() {
        return _module_federation_sdk.loadScript;
      }
    });
    Object.defineProperty(exports2, "loadScriptNode", {
      enumerable: true,
      get: function() {
        return _module_federation_sdk.loadScriptNode;
      }
    });
    exports2.matchRemoteWithNameAndExpose = require_manifest2.matchRemoteWithNameAndExpose;
    exports2.registerGlobalPlugins = require_global2.registerGlobalPlugins;
    exports2.resetFederationGlobalInfo = require_global2.resetFederationGlobalInfo;
    exports2.safeWrapper = require_tool2.safeWrapper;
    exports2.satisfy = require_index.satisfy;
    exports2.setGlobalFederationConstructor = require_global2.setGlobalFederationConstructor;
    exports2.setGlobalFederationInstance = require_global2.setGlobalFederationInstance;
    Object.defineProperty(exports2, "types", {
      enumerable: true,
      get: function() {
        return require_index$3.type_exports;
      }
    });
  }
});

// node_modules/.pnpm/@module-federation+runtime@2.2.2/node_modules/@module-federation/runtime/dist/utils.cjs
var require_utils4 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime@2.2.2/node_modules/@module-federation/runtime/dist/utils.cjs"(exports2) {
    var require_runtime4 = require_runtime();
    var _module_federation_runtime_core = require_dist3();
    function getBuilderId() {
      return typeof FEDERATION_BUILD_IDENTIFIER !== "undefined" ? FEDERATION_BUILD_IDENTIFIER : "";
    }
    function getGlobalFederationInstance(name, version) {
      const buildId = getBuilderId();
      return _module_federation_runtime_core.CurrentGlobal.__FEDERATION__.__INSTANCES__.find((GMInstance) => {
        if (buildId && GMInstance.options.id === buildId) return true;
        if (GMInstance.options.name === name && !GMInstance.options.version && !version) return true;
        if (GMInstance.options.name === name && version && GMInstance.options.version === version) return true;
        return false;
      });
    }
    exports2.getGlobalFederationInstance = getGlobalFederationInstance;
  }
});

// node_modules/.pnpm/@module-federation+runtime@2.2.2/node_modules/@module-federation/runtime/dist/index.cjs
var require_dist4 = __commonJS({
  "node_modules/.pnpm/@module-federation+runtime@2.2.2/node_modules/@module-federation/runtime/dist/index.cjs"(exports2) {
    Object.defineProperty(exports2, Symbol.toStringTag, { value: "Module" });
    var require_runtime4 = require_runtime();
    var require_utils5 = require_utils4();
    var _module_federation_runtime_core = require_dist3();
    var _module_federation_error_codes = require_dist2();
    function createInstance(options) {
      const instance = new ((0, _module_federation_runtime_core.getGlobalFederationConstructor)() || _module_federation_runtime_core.ModuleFederation)(options);
      (0, _module_federation_runtime_core.setGlobalFederationInstance)(instance);
      return instance;
    }
    var FederationInstance = null;
    function init(options) {
      const instance = require_utils5.getGlobalFederationInstance(options.name, options.version);
      if (!instance) {
        FederationInstance = createInstance(options);
        return FederationInstance;
      } else {
        instance.initOptions(options);
        if (!FederationInstance) FederationInstance = instance;
        return instance;
      }
    }
    function loadRemote(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.loadRemote.apply(FederationInstance, args);
    }
    function loadShare(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.loadShare.apply(FederationInstance, args);
    }
    function loadShareSync(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.loadShareSync.apply(FederationInstance, args);
    }
    function preloadRemote(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.preloadRemote.apply(FederationInstance, args);
    }
    function registerRemotes(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.registerRemotes.apply(FederationInstance, args);
    }
    function registerPlugins(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.registerPlugins.apply(FederationInstance, args);
    }
    function getInstance() {
      return FederationInstance;
    }
    function registerShared(...args) {
      (0, _module_federation_runtime_core.assert)(FederationInstance, _module_federation_error_codes.RUNTIME_009, _module_federation_error_codes.runtimeDescMap);
      return FederationInstance.registerShared.apply(FederationInstance, args);
    }
    (0, _module_federation_runtime_core.setGlobalFederationConstructor)(_module_federation_runtime_core.ModuleFederation);
    Object.defineProperty(exports2, "Module", {
      enumerable: true,
      get: function() {
        return _module_federation_runtime_core.Module;
      }
    });
    exports2.ModuleFederation = _module_federation_runtime_core.ModuleFederation;
    exports2.createInstance = createInstance;
    exports2.getInstance = getInstance;
    Object.defineProperty(exports2, "getRemoteEntry", {
      enumerable: true,
      get: function() {
        return _module_federation_runtime_core.getRemoteEntry;
      }
    });
    Object.defineProperty(exports2, "getRemoteInfo", {
      enumerable: true,
      get: function() {
        return _module_federation_runtime_core.getRemoteInfo;
      }
    });
    exports2.init = init;
    exports2.loadRemote = loadRemote;
    Object.defineProperty(exports2, "loadScript", {
      enumerable: true,
      get: function() {
        return _module_federation_runtime_core.loadScript;
      }
    });
    Object.defineProperty(exports2, "loadScriptNode", {
      enumerable: true,
      get: function() {
        return _module_federation_runtime_core.loadScriptNode;
      }
    });
    exports2.loadShare = loadShare;
    exports2.loadShareSync = loadShareSync;
    exports2.preloadRemote = preloadRemote;
    Object.defineProperty(exports2, "registerGlobalPlugins", {
      enumerable: true,
      get: function() {
        return _module_federation_runtime_core.registerGlobalPlugins;
      }
    });
    exports2.registerPlugins = registerPlugins;
    exports2.registerRemotes = registerRemotes;
    exports2.registerShared = registerShared;
  }
});
export default require_dist4();
//# sourceMappingURL=@module-federation_runtime.js.map
